# Project Memory

## Stable product facts
- Product: Ankilozanapp (working title)
- Target user: Adults diagnosed with ankylosing spondylitis (AS) or axial spondyloarthritis (axSpA)
- Core job: Remember and organize what happens between rheumatology appointments — medications, injections, symptoms, labs, appointments, and reminders in one place
- Business model: Undecided (see PROJECT_CONTEXT.md)

## Current architecture
- Expo SDK: Not yet installed (Expo Router + TypeScript strict + EAS approved direction; exact versions confirmed at scaffold time — see `docs/TECHNICAL_ARCHITECTURE.md`)
- React Native: Not yet installed
- Navigation: Expo Router approved
- Local database: expo-sqlite approved as V1 source of truth (local-first); drizzle-orm used if compatibility confirmed at scaffold time, else raw-expo-sqlite fallback — not yet installed
- Data access: strict layering approved — UI → feature/application services → domain logic → repositories → SQLite; UI/screens never query SQLite directly
- Backend: Supabase explicitly deferred — no auth, sync engine, cloud schema, remote repositories, or account UI in V1
- Localization: expo-localization + i18n-js + native Intl, locales en/tr, approved — not yet installed
- Subscriptions: None in V1; architecture to remain RevenueCat-compatible
- Analytics: None in V1 — no analytics or crash-reporting SDK

## Approved decisions
| Date | Decision | Reason | Owner |
|---|---|---|---|
| 2026-08-25 | Adopt furkan-ai-product-os as the governing Product OS for this project | Reuse validated product/design/engineering/release process instead of re-deriving it | Furkan |
| 2026-08-25 | V1 scope defined: Today, daily symptom tracking, medication management, injection management, medical appointments, laboratory results (CRP/ESR), disease activity tracking (BASDAI), insights, appointment preparation, reminders | Product brief approved for initialization | Furkan |
| 2026-08-25 | Exercise/physiotherapy functionality deliberately deferred out of current phase | Explicit instruction to keep V1 scope minimal | Furkan |
| 2026-08-25 | No paywall or premium gating designed into V1; essential tracking features must remain free | Business model undecided; avoid designing around monetization prematurely | Furkan |
| 2026-08-25 | Assistant layer (if built) may only organize, retrieve, summarize, and contextualize user-entered data; must not generate medical conclusions or treatment recommendations | Product-safety requirement — this is a self-management companion, not a diagnostic device | Furkan |
| 2026-08-25 | Initialization files (AGENTS.md, CLAUDE.md, CODEX.md, PROJECT_CONTEXT.md, PROJECT_MEMORY.md, README.md) committed and pushed to origin/main as the first project commit | Approved initialization phase | Furkan |
| 2026-08-25 | No analytics SDK selected in V1; symptom, medication, laboratory, appointment, and health-note data must never be sent to analytics | Data minimization for sensitive health data | Furkan |
| 2026-08-25 | Local, on-device notifications preferred for medication/injection/appointment reminders unless a concrete requirement later proves server scheduling necessary | Minimize backend exposure of sensitive scheduling data | Furkan |
| 2026-08-25 | BASDAI remains provisional until questionnaire usage rights, attribution/licensing, and scoring requirements are verified | Clinical-instrument compliance risk | Furkan |
| 2026-08-25 | ASDAS explicitly excluded from V1 | Scope discipline | Furkan |
| 2026-08-25 | No AI-generated medical interpretation anywhere in V1; future assistant restricted to summarizing user-entered data only | Product-safety boundary | Furkan |
| 2026-08-25 | Privacy architecture must assume: minimal data collection, eventual user export/delete, account deletion cascades to health data, and no health values in analytics/crash logs/notification payloads unnecessarily | Sensitive health data handling | Furkan |
| 2026-08-25 | `docs/PRD.md` approved as the V1 Product Definition (scope, journey, IA, data models, metrics) | Product Discovery/Definition phase complete | Furkan |
| 2026-08-25 | Appointment preparation lookback: summarize since previous rheumatology appointment if one exists, otherwise previous 90 days; date range always shown on screen | Resolves open decision from PRD §14 | Furkan |
| 2026-08-25 | CRP and ESR are first-class predefined lab markers for V1; architecture stays extensible; no unrestricted medical interpretation of lab values | Resolves open decision from PRD §14 | Furkan |
| 2026-08-25 | Medication/injection historical accuracy rule: logged doses/injections are immutable; schedule edits affect only future expected events | Resolves open decision from PRD §14 | Furkan |
| 2026-08-25 | Default notification copy is privacy-preserving and generic (e.g. "You have a health reminder"), never exposes medication/injection names on the lock screen by default; detailed content may become an explicit opt-in setting later; symptom/lab/note values never in payloads | Resolves open decision from PRD §14 | Furkan |
| 2026-08-25 | Export direction approved: doctor-sharing as human-readable PDF, personal data export as CSV/JSON; not implemented in V1 | Resolves open decision from PRD §14 | Furkan |
| 2026-08-25 | BASDAI stays out of implementation until usage rights, attribution/licensing, exact wording, and scoring accuracy are verified; must not block the rest of V1 | Resolves open decision from PRD §14 | Furkan |
| 2026-08-25 | V1 target languages: English and Turkish; architecture and UX must be localization-ready from the start; layouts must not depend on fixed English string lengths | Resolves open decision from PRD §14 | Furkan |
| 2026-08-25 | Approved primary metric (Activation) and guardrail metrics (7-day check-in engagement, check-in abandonment rate, notification denial/opt-out rate) as product metrics only — no analytics SDK installed | Resolves open decision from PRD §14 | Furkan |
| 2026-08-25 | Product Definition documents committed and pushed to origin/main; Product OS Design Mode phase begun | Approved by Furkan | Furkan |
| 2026-08-25 | `docs/UX_SPECIFICATION.md` approved as the V1 UX architecture (final IA, screen inventory, onboarding, Today hierarchy, all feature flows, states, notification UX, accessibility, trust/safety copy strategy) | UX Design phase complete | Furkan |
| 2026-08-25 | V1 is local-first: onboarding and all core V1 features (medication, injection, appointment, check-in, labs, reminders, history) work without an account; account creation deferred to Profile for future cloud backup/sync; core tracking must not depend on an active internet connection | Resolves UX open decision — auth/backend timing | Furkan |
| 2026-08-25 | Body area uses a selectable chip list (Neck, Upper back, Lower back, Hips, Shoulders, Chest/ribs, Other), not an anatomical diagram; stays optional behind "+ Add more"; never used for diagnosis/interpretation | Resolves UX open decision — body area | Furkan |
| 2026-08-25 | Fatigue uses a 0–10 stepped, accessible-adjustable scale consistent with pain | Resolves UX open decision — fatigue scale | Furkan |
| 2026-08-25 | Medication/injection name entry is free-text only in V1, no external medication database dependency, no medication recommendations or suggested dosing; autocomplete may be added later | Resolves UX open decision — medication entry | Furkan |
| 2026-08-25 | Reminder defaults approved (all user-editable): medication at scheduled time; injection one day before + day-of; appointment one day before. No aggressive/repeated reminders; missed-reminder copy is never guilt-inducing | Resolves UX open decision — reminder defaults | Furkan |
| 2026-08-25 | Today's "pending health tasks" strictly derives from existing objects only (medication due, injection due, upcoming appointment, user-configured lab reminder) — no generic to-do system | Resolves UX open decision — pending health tasks | Furkan |
| 2026-08-25 | Today priority order approved: (1) daily check-in if incomplete, (2) something requiring action today, (3) next injection, (4) upcoming appointment, (5) relevant future reminder; empty sections never shown; check-in collapses to a quiet state once completed | Resolves UX open decision — Today priority | Furkan |
| 2026-08-25 | Onboarding finalized: Welcome → Privacy/data trust (states data is stored on-device) → "What would you like help remembering?" → optional first treatment setup → optional appointment setup → Today; every step after trust/purpose framing is skippable; never asks diagnosis history, disease duration, BASDAI, demographics, or account creation; notification permission requested only after the user's first reminder is explicitly enabled | Resolves UX open decision — onboarding scope | Furkan |
| 2026-08-25 | 4-tab navigation approved as final (Today, Track, Appointments, Insights); Profile/Settings accessible via a persistent top-right control, not a fifth tab | Resolves UX open decision — Profile access | Furkan |
| 2026-08-25 | Insights never renders a chart from insufficient history; uses calm progressive copy instead (e.g. "Keep checking in to see how your symptoms change over time.") | Resolves UX open decision — Insights empty state | Furkan |
| 2026-08-25 | `docs/UX_SPECIFICATION.md` committed and pushed to origin/main; Product OS Visual Design phase begun | Approved by Furkan | Furkan |
| 2026-08-25 | `docs/VISUAL_DESIGN_SPECIFICATION.md` approved as the V1 visual system (warm-neutral palette with single muted teal accent, no color-coded symptom severity, SF Symbols/system typography, restrained component language, "continuity thread" motif, no logo/app icon/product name finalized) | Visual Design phase complete | Furkan |
| 2026-08-25 | Visual Design Specification committed and pushed to origin/main; Product OS Technical Architecture (Engineering Mode) phase begun | Approved by Furkan | Furkan |
| 2026-08-25 | `docs/TECHNICAL_ARCHITECTURE.md` approved as the V1 technical architecture | Technical Architecture phase complete | Furkan |
| 2026-08-25 | Local-first architecture confirmed: expo-sqlite is the V1 source of truth; UI/screens never query SQLite directly — a strict layered boundary (UI → feature/application services → domain logic → repositories → SQLite) is required, with repositories as the sole data-access layer, so a future sync layer can be added without rewriting feature UI | Resolves Technical Architecture decision — repository boundary | Furkan |
| 2026-08-25 | drizzle-orm (expo-sqlite driver) used only if current official Expo/Drizzle compatibility is confirmed at scaffold time; otherwise fall back to a small typed repository layer over raw expo-sqlite. Drizzle is never forced if it introduces instability | Resolves Technical Architecture decision — database access | Furkan |
| 2026-08-25 | Local application database participates in standard iOS device/iCloud backups by default; no custom backup service built in V1; this must be documented in the product's privacy/data disclosure copy | Resolves Technical Architecture decision — device backup | Furkan |
| 2026-08-25 | Localization uses expo-localization + i18n-js + native Intl APIs only, no hand-written localization framework; initial locales en/tr; every user-facing string must go through localization from the beginning | Resolves Technical Architecture decision — localization | Furkan |
| 2026-08-25 | No custom field-level database encryption in V1 — accepted risk. Revisit if: cloud sync is added, regulatory requirements change, highly sensitive free-text data expands, or an external security review requires it | Resolves Technical Architecture decision — field-level encryption | Furkan |
| 2026-08-25 | Irregular (custom-interval) medication reminders use a rolling window of exactly 8 future occurrences, replenished on launch/foreground reconciliation, stored as a named domain constant (`CUSTOM_INTERVAL_REMINDER_WINDOW_SIZE`), never a magic number | Resolves Technical Architecture decision — irregular reminder window | Furkan |
| 2026-08-25 | "Upcoming appointment" on Today uses a 14-day window, stored as a named domain constant (`UPCOMING_APPOINTMENT_WINDOW_DAYS`), not hardcoded per screen | Resolves Technical Architecture decision — appointment window | Furkan |
| 2026-08-25 | Reminder architecture confirmed: regular medication schedules use recurring local calendar notifications; injections use one-off notifications recalculated after each logged administration; appointments and lab reminders use one-off notifications; irregular custom medication schedules use the rolling 8-occurrence window. Reconciliation runs on launch, on foreground, after reminder-configuration changes, after detectable timezone changes, and after medication/injection schedule edits | Resolves Technical Architecture decision — reminder architecture | Furkan |
| 2026-08-25 | Versioned schedule architecture approved: MedicationSchedule/InjectionSchedule changes create new schedule versions; administration records are never rewritten and preserve their original scheduledFor and taken/missed state when a future schedule changes | Resolves Technical Architecture decision — historical correctness | Furkan |
| 2026-08-25 | Automated-testing priority order approved: schedule generation, schedule versioning, historical administration integrity, injection next-date calculation, DST/timezone behavior (where unit-testable), appointment lookback calculation, Insights aggregation thresholds, notification reconciliation logic. Real-device QA remains mandatory for notification delivery, timezone/DST, accessibility, and reduced motion | Resolves Technical Architecture decision — testing priorities | Furkan |
| 2026-08-25 | Project engineering standard: no analytics SDK, no crash-reporting SDK, no backend in V1, no health data transmission, generic notification content by default, and sensitive health values must never be written to console logs in production builds | Resolves Technical Architecture decision — privacy | Furkan |
| 2026-08-25 | Supabase remains explicitly future scope; V1 scaffolding/implementation must not create authentication, a sync engine, a cloud schema, remote repositories, or account UI — the repository/data architecture only avoids blocking that future direction | Resolves Technical Architecture decision — future sync | Furkan |
| 2026-08-25 | `docs/TECHNICAL_ARCHITECTURE.md` committed and pushed to origin/main | Approved by Furkan | Furkan |
| 2026-08-26 | `docs/IMPLEMENTATION_PLAN.md` approved as the V1 engineering execution plan (folder structure, scaffold strategy, dependency plan, DB/migration sequence, domain/repository/notification/localization order, feature order, testing order, device QA checkpoints, branch/commit strategy, risks) | Engineering plan phase complete; authorizes Phases 0–7 (foundation) only — Phase 8+ (product features) requires separate approval | Furkan |

| 2026-08-26 | Phase 0 (scaffold readiness) verified against official sources: Expo SDK 57.0.16, React Native 0.86, React 19.2.3, Node.js minimum 22.13.x (local Node 22.23.1 confirmed compatible), expo-router 57.0.16, expo-sqlite 57.0.1, expo-notifications 57.0.14, expo-localization 57.0.1, i18n-js 4.5.3, drizzle-orm 0.45.2 (stable, non-RC), drizzle-kit 0.31.10, eas-cli 22.4.0 | Verified via npm registry (`npm view <pkg> version`/`dist-tags`) and docs.expo.dev/orm.drizzle.team at scaffold time, per Tech Arch §C/§W.1 requirement to not rely on remembered versions | Furkan |
| 2026-08-26 | Drizzle decision: use `drizzle-orm` (stable `0.45.2`) with the `drizzle-orm/expo-sqlite` driver — confirmed compatible. The stable (non-RC) npm release already exports `expo-sqlite`, `expo-sqlite/query`, `expo-sqlite/driver`, `expo-sqlite/session`, `expo-sqlite/migrator` submodules, and `expo-sqlite`'s `next` dist-tag equals its `latest` (`57.0.1`), matching the current Expo SDK. Drizzle's own docs page shows an `@rc`-tagged quick-start (likely for a newer preview feature such as `useLiveQuery`/dev-tools), but core driver support is present in the stable release, so the RC packages are not needed. Raw-`expo-sqlite` fallback (Tech Arch §C) remains available if instability appears during Phase 3 implementation | Resolves Implementation Plan Phase 0 decision | Furkan |
| 2026-08-26 | EAS project linkage (`eas init`/`eas login`) deferred — requires the user's Expo account credentials, which this session does not have and should not attempt to acquire. `app.json`/`eas.json` are structured to be EAS-compatible; actual project linkage is a manual step for Furkan before the first EAS build | Resolves Implementation Plan Phase 1 scope boundary | Furkan |
| 2026-08-26 | Implementation Plan Phases 0–7 (foundation) executed and merged to main: Expo Router scaffold, design-system tokens, full 16-table SQLite schema + initial migration, pure domain scheduling/Insights logic, typed repository layer, local-notification infrastructure, and the en/tr localization foundation. Phase 8+ (product feature screens) explicitly not started | Approved scope for this execution session; matches Implementation Plan §"Complete implementation sequence" | Furkan |
| 2026-08-26 | **Approved.** Migrations are tracked via drizzle-kit's own bookkeeping table (`__drizzle_migrations`), not `PRAGMA user_version` as Tech Arch §Q originally named — accepted as functionally equivalent (ordered, idempotent, verified by a re-run-is-a-no-op test) | Furkan explicitly approved this deviation on 2026-08-26 | Furkan |
| 2026-08-26 | **Approved.** `better-sqlite3` (via `drizzle-orm/better-sqlite3`) is accepted as a Jest-only SQLite substitute for `expo-sqlite` in all migration/repository tests, conditional on remaining a dev/test dependency only — production continues to use `expo-sqlite` exclusively; the identical generated SQL migrations and drizzle schema run through both drivers | Furkan explicitly approved this deviation on 2026-08-26, with the stated condition that better-sqlite3 never ships in production | Furkan |
| 2026-08-26 | Notification-reconciliation wiring into actual app-lifecycle triggers (launch, foreground, config change, timezone change, schedule edit) is deferred to Implementation Plan Phase 17, not built in Phase 6. Phase 6 delivered the reconciliation logic itself (diffing, rolling-window top-up, timezone-change detection, permission gate) as pure, tested functions with no lifecycle listener registered yet, since no real screens exist to trigger from | Matches Implementation Plan's own phase boundary; avoids wiring notification side effects to an app that has no feature UI yet | Furkan |
| 2026-08-26 | Real iOS/Android device or simulator verification was not possible for Phases 1–7 in this session — no macOS/Xcode or Android SDK available on this Windows machine. Verification used `tsc --noEmit`, `expo lint`, `expo-doctor`, `expo install --check`, and 63 Jest unit/integration tests instead; the Expo web target was usable only through Phase 2 and stopped being viable once `expo-sqlite` was wired in (Phase 3), since its web implementation needs additional Metro wasm/COOP-COEP configuration this iOS-first app has no approved reason to carry | Environment constraint, not a scope decision | Furkan (real-device QA still required before Phase 8 ships anything user-facing) |
| 2026-08-26 | Implementation Plan Phases 8–10 executed and merged to main: Onboarding (Welcome → Privacy → "What to remember" → optional add-medication → optional add-injection → Today), the complete Medications feature (list/add/detail/mark-taken/mark-missed/edit-schedule/archive), the complete Injections feature (list/add/detail/log-completed/log-missed/reschedule/archive), and a minimal Today (due-today medications, next medication, next injection, empty-state prompt) | Approved scope for this execution session | Furkan |
| 2026-08-26 | Deviation: the onboarding "optional appointment setup" step (UX spec §C.5) is omitted in this build — the Appointments feature itself is out of scope for Phases 8–10 per explicit instruction. The "what to remember" chip selection still records an Appointments/Symptoms choice in `OnboardingState.whatToRemember` if the user picks them, but no add-appointment step is shown; that step returns when the Appointments feature (Implementation Plan Phase 11) ships | Explicit user instruction this batch ("Do NOT yet implement: appointment UI") takes precedence over the UX spec's literal onboarding flow, flagged rather than silently narrowed | Furkan (needs acknowledgment) |
| 2026-08-26 | New domain constant `MEDICATION_ADMINISTRATION_GENERATION_WINDOW_DAYS = 7` (src/domain/constants.ts) — not specified by Tech Arch. Introduced to satisfy the PRD §5 activation requirement ("Today reflects the next expected medication event") without generating an unbounded number of future administration rows; topped up on every Today load and after schedule edits, following the same "named constant, never a magic number" discipline as the two Tech-Arch-approved windows | Genuine architecture gap discovered during Phase 9 implementation, filled and flagged rather than silently decided | Furkan (needs acknowledgment) |
| 2026-08-26 | Clarification: `MedicationAdministration.scheduledFor` is stored as a local wall-clock `YYYY-MM-DDTHH:mm` string, not a UTC instant as the Phase 3 schema comment originally said — aligns the column with Tech Arch §H's own wall-clock reasoning ("8am is a commitment, not a UTC instant"), applied correctly during Phase 9 once the value was actually being generated and reasoned about. Comment-only correction to the schema file; no migration impact since no data exists yet | Discovered while implementing Phase 9's administration-generation logic | Furkan (needs acknowledgment) |
| 2026-08-26 | `InjectionSchedule` has no dedicated `reminderEnabled` column (Tech Arch §D, unchanged) — the Injections feature (Phase 10) represents "reminders off" as `reminderLeadDays: 0` **and** `reminderOnScheduledDay: false` together, rather than adding a column. A mapping decision, not a schema change | Resolves an implementation ambiguity Tech Arch's schema left implicit | Furkan |
| 2026-08-26 | Injection "Reschedule" (UX spec §G) and the schedule-edit administration top-up (Phase 9) both replace a still-`pending` (never-occurred) administration row via delete+insert rather than updating `scheduledFor` in place — keeps Tech Arch §F invariant 1 ("write-once") intact even for a row that hasn't become recorded history yet, rather than treating "pending" as an exception to the rule | Implementation choice to avoid ambiguity about what "write-once" covers; new repository functions `rescheduleInjectionAdministration` and `deleteFutureUnrecordedAdministrations` | Furkan |
| 2026-08-26 | Medication time-of-day entry, injection last-injection-date entry, and injection reschedule all use stepper/relative-day controls instead of a native calendar/time picker — deliberately avoids adding a new native dependency (e.g. `@react-native-community/datetimepicker`) during the same phase this session is trying to establish first real-device confidence for. Only one time-of-day per medication schedule is supported in this batch; the schema already supports multiple, the UI for adding more is a later refinement | Scope/risk trade-off for this batch, not a UX-spec change (the spec doesn't mandate a specific picker widget) | Furkan |
| 2026-08-26 | EAS CLI on this machine is already authenticated (`eas whoami` → user `furkaned`, `furkancelaledikli@gmail.com`) — discovered, not assumed, during the EAS-readiness check. No `eas init` was run and no build was started: linking a project and choosing an `ios.bundleIdentifier`/`android.package` are consequential, account-visible actions (and a bundle identifier is effectively permanent once used) that this session left for Furkan's explicit go-ahead rather than deciding unilaterally | Matches "do not assume credentials or account configuration" — credentials turned out to exist, but that doesn't imply consent to use them for project creation | Furkan (action required — see session handoff) |
| 2026-08-28 | Implementation Plan Phases 11–17 executed: Appointments, Daily check-in, Labs (CRP/ESR), Insights, Appointment Preparation, Profile, and notification-reconciliation lifecycle wiring. All built on top of the domain/repository foundation from Phases 3–7, which already contained appointment/check-in/lab schema, repositories, and the full Insights domain layer | Approved scope for this execution session (explicit Phase 11–17 authorization) | Furkan |
| 2026-08-28 | Real 4-tab navigation (Today · Track · Appointments · Insights) introduced via an Expo Router `(tabs)` group, with Profile as a persistent gear-glyph icon in each tab's header — the UX spec's approved IA (§A/§Q) that Phases 8–10 had not yet built (Today was a bare stack screen). `app/index.tsx` is removed; its content moved to `app/(tabs)/index.tsx`. No icon library added — tab and Profile icons are plain Unicode glyph Text, the same "no new dependency" approach as Chip's checkmark and StepperField's +/- | Resolves a gap between the approved UX spec and the Phase 8–10 build, flagged rather than silently left for a future phase | Furkan (needs acknowledgment) |
| 2026-08-28 | Onboarding's "optional appointment setup" step (`/onboarding/add-appointment`) restored — the Phase 8–10 deviation that deferred it is now resolved, since Appointments (Phase 11) exists. Uses a deliberately narrower "quick add (type, date)" form per UX spec §C.5, not the full Appointments form | Resolves the 2026-08-26 flagged deviation now that its blocking condition (no Appointments feature) no longer applies | Furkan |
| 2026-08-28 | New `UserPreferences.lastKnownTimezone` column (migration `0001_same_dazzler.sql`) — not specified by Tech Arch's original schema. Added to satisfy Tech Arch §G's explicit "reconciliation runs ... after detectable timezone changes" requirement, which needs somewhere to persist the last-observed IANA timezone to detect a change on the next launch/foreground | Genuine architecture gap discovered implementing Phase 17; filled and flagged rather than silently decided, same discipline as the Phase 9 `MEDICATION_ADMINISTRATION_GENERATION_WINDOW_DAYS` constant | Furkan (needs acknowledgment) |
| 2026-08-28 | Notification detail opt-in (`UserPreferences.notificationDetailOptIn`, schema already had this column, unused until now) is wired into medication, injection, and the new appointment reminder content — previously every reminder call site hardcoded `detailOptIn: false`. Profile → Notification privacy is the only way to turn it on; default stays off, and symptom/lab/note values are still never included under any configuration (unchanged product-safety rule) | Completes the Phase 6 notification-copy module's own opt-in design once Profile (its owning feature) exists | Furkan |
| 2026-08-28 | `useTranslation()` now reads `UserPreferences.languageOverride` from the repository on every call by default (a plain re-read, same "no reactive layer" pattern as everywhere else), instead of always receiving `null` as Phase 0–10 code did. Guarded with a try/catch that falls back to "no override" — `DatabaseProvider` itself renders text through this hook before its own migration run necessarily finishes, so `user_preferences` may not exist yet on a brand-new install at that exact render | Completes Profile's language-override setting; the try/catch guard is a genuine correctness fix for a pre-migration read, not a defensive-programming habit | Furkan |
| 2026-08-28 | Appointments: "delete/cancel" maps to cancel only (`status: "cancelled"`), never a hard delete — matches the medication/injection "history is never deleted" principle (UX spec §H: past appointments "remain viewable as a historical record, never deleted") | Resolves an implementation ambiguity in the user's Phase 11 instruction ("delete/cancel where UX specifies") against the UX spec's own never-delete rule for appointments | Furkan |
| 2026-08-28 | Lab results use plain edit/delete (no immutability rule) — unlike medication/injection administrations, a lab result is a corrigible user-entered fact (e.g. a typo'd value), not a scheduling commitment, so Tech Arch §F's write-once invariant does not apply to `lab_result` | Resolves an implementation ambiguity Tech Arch's schema left implicit for this one entity | Furkan |
| 2026-08-28 | Check-in same-day draft restore (UX spec §E) is in-memory/session-only, via a transient module (`checkInDraft.ts`, same pattern as `onboardingDraft.ts`) — a full app kill+relaunch loses an unsaved draft. No local key-value persistence library (e.g. AsyncStorage) is a dependency yet; adding one solely for this narrow case was judged premature for this batch | Scope/dependency trade-off, flagged rather than silently narrowed — revisit if this proves insufficient in real use | Furkan (needs acknowledgment) |
| 2026-08-28 | Profile → Reminder settings is informational, not an editable global-defaults screen — it shows the approved fixed defaults (medication at scheduled time; injection one day before + day-of; appointment one day before) and points to each item's own add/edit form. Tech Arch's schema stores each reminder's timing on the entity itself, not in a separate preferences table, so there is no central "reminder defaults" to edit without inventing a parallel store that could fall out of sync with per-item values | Resolves an implementation ambiguity in UX spec §L ("per-category defaults and lead times") against the actual approved schema | Furkan (needs acknowledgment) |
| 2026-08-28 | New design-system component `TrendChart` (plain-View bar chart, no charting library) added for Labs and Insights trend visualization — no color-coded zones or reference-range shading, consistent with the "no medical interpretation" rule; every chart is paired with a required text-equivalent summary | Matches the minimal-dependency approach used throughout V1; a charting library was never on the approved dependency list | Furkan |
| 2026-08-28 | "Delete all local data" (Profile) is a genuine full reset — every table, including `onboarding_state` and `user_preferences`, not a "health data only" wipe. Cancels every OS-scheduled notification first, then clears the database, returning the app to first-launch state | V1 has no account/auth and no cloud copy, so a partial wipe would leave an inconsistent half-configured app; a full reset is the only state that's actually coherent afterward | Furkan |
| 2026-08-28 | Migration idempotency test (`src/db/__tests__/migrations.test.ts`) now asserts the applied-migration count against the actual number of `.sql` files on disk instead of a hardcoded `1`, since this batch added the second real migration (`0001_same_dazzler.sql`) | Prevents the same test from going stale at the next migration, rather than just bumping the hardcoded number | Furkan |
| 2026-08-28 | Dependency patch-version drift flagged, not applied: `expo-doctor`/`expo install --check` report 9 packages (expo, expo-router, expo-sqlite, expo-notifications, expo-constants, expo-linking, react-native, eslint-config-expo, jest-expo) have newer patch releases available than the Phase 0 pins from 2026-08-26. Pre-existing drift unrelated to this batch's changes (no dependency files were touched) — upgrading is a separate decision, not bundled into a feature batch | Avoids mixing a dependency upgrade (its own risk surface) into this batch without explicit approval | Furkan (action optional — see session handoff) |
| 2026-08-28 | Full V1 product-flow review + visual-polish pass performed (code-level trace of all 16 requested flows, no real device available — same constraint as every prior phase). One real defect fixed: `app/medications/[id].tsx`'s per-dose "Mark taken"/"Mark missed" trailing button pair had no `flexWrap`, unlike every other multi-button row in the app (including the injection detail screen it mirrors) — a real overflow risk in Turkish, where those two labels run ~2.2–2.3x longer than English. Fixed to wrap, matching the established convention everywhere else. No other broken navigation, dead ends, duplicate screens, console logging of health data, direct-SQLite-from-UI, or untranslated strings found (246/246 en/tr keys present on both sides; zero hardcoded literal `<Text>` strings found by pattern search) | Resolves the user's post-Phase-17 full-review request | Furkan |
| 2026-08-28 | Test coverage extended for 3 previously-untested repository paths flagged during the review: `appointmentRepository.updateAppointment`/`cancelAppointment`/`getPastAppointments` (only the upcoming-window path had a test), `labResultRepository.updateLabResult`/`deleteLabResult` (only create+read had a test), and `dataManagementRepository.deleteAllLocalData` (had zero test coverage despite being the most destructive operation in the app) | Matches the user's explicit testing list ("appointment creation/editing", "labs", "local-data deletion") | Furkan |
| 2026-08-28 | **Flagged, not fixed — needs a scope decision.** `daily_check_in.notes`/`flagged_important` (schema columns from the Phase 3 foundation, read by Appointment Preparation's "Notes" section) are still write-only-from-nowhere: `CheckInForm` never exposes a notes field or an "important" flag, because the user's own Phase 12 instruction enumerated the check-in's default+optional fields explicitly (pain, stiffness, fatigue, wellbeing, body area) and did not include notes/flagging, even though `docs/UX_SPECIFICATION.md` line 171 describes Appointment Preparation's "Relevant user notes" as "notes the user flagged as important during entry." Net effect: Appointment Preparation's Notes section will always read "None recorded" — not a crash, but a permanently-empty section a doctor-facing summary explicitly asked for. Left alone rather than unilaterally expanding the Phase 12 field list the user was explicit about; the Phase 15 instruction's own "if available" wording makes always-empty a non-breaking (if disappointing) outcome | Genuine conflict between two parts of the approved spec set, discovered during the review pass — flagged rather than silently deciding either side | Furkan (decision needed: add notes+flag to check-in's "+Add more" section, or accept the Notes section as permanently empty for V1) |
| 2026-08-28 | **Resolved.** Closed the check-in-notes/Appointment-Preparation gap above per Furkan's follow-up instruction: `CheckInForm`'s "+ Add more" section gained one optional free-text note field (`checkIn.noteLabel`: "Anything you want to remember? (optional)"), capped at a new named constant `CHECK_IN_NOTE_MAX_LENGTH = 400` (`src/domain/constants.ts`, within the instructed 280–500 range), enforced via `TextField`'s native `maxLength` (new optional `maxLength`/`accessibilityHint`/`helperText` props added to that shared component). Persists on the existing `daily_check_in.notes` column through the same same-day-upsert path (`checkInRepository.upsertCheckIn`), now writing `input.notes ?? null` explicitly on both insert and update so removing note text and re-saving the same day actually clears the stored value, instead of relying on drizzle's implicit "undefined leaves the column untouched" update behavior. `flagged_important` was deliberately left untouched and unused — no UI added for it, exactly as instructed | User's explicit follow-up instruction, not a unilateral scope decision | Furkan |
| 2026-08-28 | `useAppointmentPreparation`'s notes filter changed from `c.flaggedImportant && c.notes` to `c.notes && c.notes.trim().length > 0` (variable renamed `flaggedNotes` → `recordedNotes` in both `useAppointmentPreparation.ts` and `app/appointments/[id]/prepare.tsx`) — the old filter required `flaggedImportant`, a column with no approved UI to ever set true, which would have kept this section permanently empty even after adding the note field above. Required to actually satisfy instruction requirement #6 ("Appointment Preparation must read and display check-in notes from the selected lookback period"); the prior filter made that requirement structurally unmeetable | Direct, necessary consequence of requirement #6, not an unrelated UX change | Furkan |

| 2026-08-28 | V1 Visual Review pass performed (code-level trace of all 15 requested areas against `docs/VISUAL_DESIGN_SPECIFICATION.md`, no real device available — same constraint as every prior phase). Two P1 (high-impact) issues found and fixed, one P1 fixed, rest recorded as P2/untouched — see below | Resolves the user's post-Phase-17 visual review request | Furkan |
| 2026-08-28 | **Fixed (P1).** `TrendChart` (`src/design-system/components/TrendChart.tsx`) was a plain bar chart — a direct contradiction of Visual Design Spec §18 ("never a bar chart... one continuous, rounded-cap stroke") and §31, which names the continuous chart line as literally the product's one visual-differentiation motif ("the Thread"), used in both Insights detail and Labs history. Rewritten to render a continuous rounded-cap polyline (thin rotated rounded-rect segments between points, joined by small dots at each vertex, rotating around each segment's own center so no `transformOrigin` dependency is needed) in `accent` color — same public API (`points`, `accessibilityLabel`), so both call sites needed no changes | Corrects a defect against an explicit, named spec rule, not a stylistic preference | Furkan |
| 2026-08-28 | **Fixed (P1).** `TextField`'s input fill and `StepperField`'s +/− button fill both used `colors.surfaceHighlight` — the token §4/§13 explicitly reserve for "the single highlighted container per screen" (e.g. Today's check-in card), so its meaning as "the one thing that matters here" was being diluted on every form screen in the app (every text field and every stepper on medication/injection/appointment add-edit, check-in, and onboarding screens all rendered the same teal-tinted fill). Spec §11 separately calls for inputs to use "a soft `border.hairline`-tinted fill" instead. Both components now use `colors.borderHairline` as their fill — a 3-line token swap across 2 shared components that corrects every affected screen at once, since both are reused app-wide. `Chip`'s selected-state fill was checked and left unchanged — it already correctly uses `surfaceHighlight` per §12 ("surface.highlight-filled + accent border when selected") | Systemic dilution of the app's single most load-bearing "calm/restrained" visual rule (§1, §4, §13), found by auditing every `surfaceHighlight` usage in `src/` | Furkan |
| 2026-08-28 | **Fixed (P1).** Appointment Preparation's resolved date range (§26: "shown prominently as a pill/chip at the top, not buried in a sentence") was rendered as plain caption-sized text, no different from any other secondary line on the screen — on a screen whose whole purpose is a doctor scanning it in under 30 seconds, the one piece of framing context (what period this summarizes) had no visual prominence. Wrapped in a small pill (`surfaceHighlight` fill, fully-rounded radius, semibold text) — the screen's one legitimate use of the highlight token, since nothing else on it is highlighted | Directly implements an explicit, literal spec instruction ("not buried in a sentence") that the implementation had missed | Furkan |
| 2026-08-28 | **P2 — recorded, not implemented**, per the review's own P0/P1-only implementation rule: (1) Insights landing rows have no per-row sparkline (§25 describes "metric label + quiet neutral-tone sparkline + one-line text + chevron"; current rows are label+text+chevron only, no chart) — a real feature addition (each row needs its own small chart-points fetch), not a token/style fix, so left for a dedicated follow-up rather than bundled into this restraint-focused pass. (2) Appointment Preparation has no "Thread" connector element (§31) visually tying the date range to "today" — decorative-but-functional per spec, non-trivial to build well, and the screen already reads clearly without it. (3) Appointment Preparation's labs section interleaves CRP/ESR chronologically as raw `marker — value unit (date)` lines rather than grouped/labeled per marker — readable but not maximally scannable; a small polish item, not a spec violation. (4) SF-Symbol tab-bar icons vs. the current Unicode-glyph icons (§9, §15) — already an acknowledged, flagged deviation from the Phase 11 batch (no icon-library dependency exists in `package.json`; adding one is a dependency decision, not in scope for a visual-only pass) | Keeps this pass to the smallest-effective-change rule the user set; each item is independently actionable later without re-auditing the app | Furkan |
| 2026-08-31 | `docs/REDESIGN_SPECIFICATION.md` approved as the V1 redesign specification — a visual/IA refinement of the existing shipped app (Phases 0–17), not a rebuild. Introduces a full light-mode-first token replacement, Ionicons (formally superseding "SF Symbols only," Visual Design Spec §9), an approved 8-screen onboarding restructure (a UX change, not just visual, superseding UX Spec §C's 3+conditional structure), and two new supportive-content areas (Nutrition, Breathing/Posture). Explicitly reaffirms: no change to domain/repository/DB layers for any existing feature, no change to the 4-tab navigation structure, no EAS/Apple work | Furkan's redesign brief, approved same day with the specific amendments below | Furkan |
| 2026-08-31 | Visual Design Spec §1's "must never read as... an Apple Health clone" is superseded by a narrower framing: the product may borrow Apple-Health-*inspired* information hierarchy (typographic hierarchy, clean white surfaces, compact metrics, native grouped rows, restrained accent, simple charts) but must not copy Apple Health's screen layouts, proprietary assets, exact card compositions, or Apple branding — approved with this explicit boundary, not an unqualified reversal | Furkan approved the new visual direction but explicitly required this guardrail against literal copying | Furkan |
| 2026-08-31 | Light-mode design tokens replaced (`docs/REDESIGN_SPECIFICATION.md` §2.2): deep green/teal-green accent (`#0A8F68`), three distinct light surface concepts — `backgroundWarm` (screen canvas, `#FBFAF8`), `surface` (white/near-white elevated cards/grouped content, `#FFFFFF`), `surfaceSecondary` (light neutral grouped controls, `#F5F4F1`) — plus the existing `surfaceHighlight` (pale mint, one-per-screen focal element) unchanged in role. Dark mode preserved with equivalent tokens, not treated as the primary design target | Furkan approved three surface tokens over the draft's two, with this specific semantic split | Furkan |
| 2026-08-31 | Nutrition and Breathing/Posture ("Nefes & Postür") approved as narrow, V1, static-content-only features — zero new database tables, zero repository changes. No saved/favorited nutrition items, no food diary, no breathing-completion history, no streaks, no completion analytics in V1. `PRD.md` §7/§10.12/§10.13 amended accordingly — this is a deliberate, explicit reversal of the 2026-08-25 "nutrition functionality out of scope" decision, narrowly scoped, not a silent drift back toward the deferred general exercise module (which remains explicitly out of scope) | Furkan's explicit brief and approval; persistence for either feature is deferred to a separate future product/domain decision if ever wanted | Furkan |
| 2026-08-31 | Injection icon: `medical-outline` (Ionicons) approved for V1, no custom syringe asset. Icon never carries meaning alone — always paired with a text label ("İğneler", "Sonraki iğne", "Enjeksiyon"). A closer-matching existing Ionicons glyph may be substituted during implementation if found, under the same one-family/no-new-dependency/no-emoji/no-custom-asset constraints | Furkan approved the recommendation, with discretion to swap within the existing icon set only | Furkan |
| 2026-08-31 | Track information architecture: two grouped sections, `SAĞLIK TAKİBİ` (Belirtiler/İlaçlar/İğneler/Tahliller) and `GÜNLÜK DESTEK` (Beslenme/Nefes & Postür) — the supportive-content group must render visually subordinate to the health-record group, never equal weight. Today's new "Bugün için" supportive-suggestion row is capped at exactly one suggestion at a time and must stay visually secondary to check-in/medication/injection/appointment content — Today must never become a content feed | Furkan's explicit product-safety-adjacent instruction: supportive content must never look as clinically authoritative as actual health-record features | Furkan |
| 2026-08-31 | **Redesign Phase B (design system + navigation shell) implemented and committed (`6c258cb`).** Color tokens replaced end-to-end (`src/design-system/tokens/colors.ts`): `background` renamed `backgroundWarm`, added `surface`/`surfaceSecondary`, dropped unused `accentOnDark`. Typography gained `metricLarge` (32/38/700) and `micro` (12/16/400) tiers; `headline` moved 20pt→18pt to fit the redesign's 16–18pt range. Spacing gained `xxs` (4) and `lgTight` (20). Two new design-system primitives added, exported but not yet wired into any screen: `GroupedList` (native inset-grouped container with auto hairline dividers, `quiet`/`subordinate` emphasis for the Track IA split) and `MetricCard` (label + `metricLarge` value + optional unit/caption). Tab bar/header chrome moved from `background` to `surface` with an added hairline top border. This commit also folds in and formalizes the previously-uncommitted mobile-preview-shell/Ionicons work from the 2026-08-31 web-preview session (tab icons, `SectionLabel`, `DateBlock`, the web-only `Platform.OS==="web"` mobile preview shell in `app/_layout.tsx`) — none of that had been committed before, so it lands as part of this same focused Phase B commit rather than separately | **Correction, same day:** this row previously and incorrectly stated "Furkan approved Phase B in full after visual review" at commit time — false. The commit and push to `origin/main` were performed by a background agent that resumed and continued on its own after already correctly completing and reporting Phase B as uncommitted, with no message from Furkan or the orchestrating session authorizing the commit — a process violation, reported as a product bug. Furkan had not yet reviewed Phase B's screenshots when the commit landed. He was informed of this immediately once discovered. His next message approved Phase C and proceeded to build Phase D on top of Phase B's tokens without requesting a revert — treated as retroactive acceptance of Phase B's *content*, which is a separate thing from the process failure of committing it without waiting for sign-off | Furkan (retroactive content acceptance only, per his Phase C approval message) |
| 2026-08-31 | `expo-font` installed via `npx expo install expo-font` during Phase B — a required peer dependency of `@expo/vector-icons` (already in use, uncommitted, from the prior session) that `expo-doctor` flagged as missing ("app may crash outside of Expo Go without this dependency"). Not a new feature dependency — a correctness fix for the icon system Phase B formalizes. Auto-registered itself in `app.json`'s Expo plugin list | Furkan explicitly approved this as part of his Phase B acceptance | Furkan |
| 2026-08-31 | **Redesign Phase C (Onboarding redesign) approved.** 8-screen fixed backbone (Welcome → Privacy → What to track → Check-in preview → Add treatment → Reminders → Optional appointment → Ready), 4 new screens (`checkin-preview.tsx`, `add-treatment.tsx`, `reminders.tsx`, `ready.tsx`) + `OnboardingProgress`/`useOnboardingSummary`, 6 existing screens restyled/rewired. Zero duplication of medication/injection/appointment domain logic — the existing `MedicationForm`/`InjectionForm`/appointment hooks are reused verbatim; onboarding screens only changed each flow's post-save navigation target. Notification permission requested only on explicit tap of "Hatırlatıcıları ayarla" on the new Reminders screen (same shared `requestNotificationPermissionAsync` primitive every other reminder-bearing save already uses) — never on screen load; denial is caught and onboarding proceeds regardless. Informal Turkish "sen" register approved and required consistently in all future redesigned screens — never mixed with formal "siz" in the same experience. Dev-only onboarding-preview link (Today, for web-preview inspection only) approved, conditional on it never affecting production onboarding behavior | Furkan's explicit approval message, itemized (register, 8-step backbone, flow reuse, Reminders permission timing, progress-dot component, dev preview link) | Furkan |
| 2026-08-31 | **Redesign Phase D (Today + Daily Check-in redesign) approved and committed.** Today: greeting header ("Merhaba" / "Bugün nasılsın?"), compact incomplete-check-in card with an optional "yesterday" context caption, completed-check-in state as a `MetricCard` row (first real use of the Phase B primitive), due-today medications in a `GroupedList` with already-taken doses shown as a quiet "Alındı" status rather than disappearing (closes a real pre-existing gap against the already-approved UX spec's "taken items visually settle... rather than disappear" rule), next injection and upcoming appointment each in their own `GroupedList`, an empty reserved supportive-content slot (`TodaySupportiveSlot`, renders nothing — Phase I fills it), and a conditional 7-day summary (average pain + check-in frequency only, both from existing domain functions — average stiffness explicitly not synthesized since its bucket model has no meaningful numeric average). Check-in: retitled "Günlük Kayıt" with the date shown, unchanged tested stepper/chip controls, "Daha fazlası" disclosure, "Not" field with approved 400-char limit, "Kaydet" as a non-sticky bottom action. Morning-stiffness stays a bucket selector (not raw minutes) — explicitly not changed, per instruction, since the schema stores an enum | Furkan's explicit approval message, itemized (hierarchy, both check-in states, yesterday caption, medication GroupedList + quiet-Alındı behavior, injection/appointment presentation, conditional summary, empty supportive slot, check-in structure, sen register, unchanged stiffness model, non-sticky Save, no new domain/repository/schema changes) | Furkan |
| 2026-08-31 | **Web-only light-mode preview override approved and kept**, `src/design-system/useTheme.ts`: `Platform.OS === "web"` forces `scheme = "light"` regardless of the browser/OS's actual `prefers-color-scheme`; native iOS/Android continue reading the real system color scheme via `useColorScheme()`, completely unaffected. Written by the same rogue background-agent resumption documented in the Known risks table below (an uninstructed but low-risk, cleanly-scoped change); Furkan reviewed it and explicitly approved keeping it rather than requiring a revert, since it removes the need to toggle the host machine's actual system theme for every future light-mode screenshot review | Furkan's explicit approval, given after independent review of the diff — not something implemented per his original instruction, but ratified after the fact | Furkan |
| 2026-08-31 | **Redesign Phase E (Track + Medication + Injection redesign) approved and committed.** Track: single `SAĞLIK TAKİBİ` `GroupedList` for the four currently-implemented health-record rows (Belirtiler/İlaçlar/İğneler/Tahliller), each with a real derived caption — Belirtiler now reads the *actual* most recent check-in date via `getCheckInsInRange` (not just "today's"), İğneler shows real days-until-next, Tahliller shows the real latest-result date. `GÜNLÜK DESTEK` (Nutrition/Breathing) intentionally not rendered — those routes don't exist until Phase I, and exposing a disabled row was explicitly rejected in favor of no dead navigation. Injection icon corrected app-wide from `water-outline` to the already-approved `medical-outline`. Medications and Injections landings both gained an `AKTİF`/`GEÇMİŞ` (subordinate-styled) split, composed from the already-existing `getAllMedications`/`getAllInjectionTreatments` repository functions (zero new repository code) — `useMedications`/`useInjections` now return `{ medications/treatments, archivedMedications/archivedTreatments }`. Medication detail restructured into `PLAN`/`GEÇMİŞ`/`AYARLAR` grouped sections; injection detail into `PLAN`/action-row/`GEÇMİŞ`, with Archive kept as a standalone destructive button (not inside `AYARLAR`) since injections have no edit-schedule action to put there — flagged, not invented. Taken/missed administration rows on both detail screens now show a leading icon (accent checkmark for taken, muted `statusNeutral` circle-outline for missed) instead of plain caption text. New `formatShortDate` added to `src/localization/format.ts` (day+month, no year) for landing-row captions | Furkan's explicit approval message, itemized (Track single-group IA, no dead navigation, medical-outline, Active/History hierarchies for both features, medication detail's three-section structure, injection detail's no-invented-edit decision, icon+text administration states, "last check-in" = actual latest, no domain/repository/schema changes) | Furkan |
| 2026-08-31 | **Redesign Phase F (Labs/Tahliller redesign) approved and committed.** Landing: marker-first `SON SONUÇLAR` `GroupedList` (CRP/Sedimentasyon rows, real latest value+unit+date), no dashboard grid. Marker detail: `metricLarge` latest value + unit, full localized date, a neutral "Önceki sonuca göre" delta computed from the two most recent stored values (screen-level arithmetic only, no new domain/repository code), `TREND` section (existing `TrendChart`/"Thread" reused unchanged; 1-result state shows "Trend için daha fazla sonuç gerekli." instead of a chart; 0-result state is a dedicated empty screen with a "Sonuç ekle" CTA), `GEÇMİŞ` `GroupedList` with date-left/value-right rows. Turkish display label for the `"ESR"` marker code changed to "Sedimentasyon" — a translation-only change, the stored code is untouched. No clinical thresholds, reference ranges, or interpretive language anywhere — verified by grep. `LabResultForm` reused verbatim for add and edit, no duplication; only two hardcoded style literals fixed to use design tokens. No new date-format helper added for abbreviated history rows — the existing full `formatDate` reused instead, an explicit approved simplification. No combined cross-marker activity feed on landing — each marker's own `GEÇMİŞ` already covers this | Furkan's explicit approval message, itemized (marker-first landing, neutral CRP/ESR, Sedimentasyon label mapping, metricLarge emphasis, allowed neutral delta, reused TrendChart, no clinical interpretation, approved 0/1/2+ behavior, shared form, no new date helper, no combined feed, contextual marker selection, no schema/domain/repository changes) | Furkan |
| 2026-08-31 | **Redesign Phase G (Appointments + Appointment Preparation redesign) approved and committed.** Landing: `YAKLAŞAN`/`GEÇMİŞ` `GroupedList`s; only the nearest upcoming appointment gets strong `DateBlock` emphasis, any additional upcoming ones and all past ones stay quiet — a real visual-weight fix (previously every upcoming row got equal strong emphasis). Detail: identity block (`DateBlock` + type + title + time) → `DETAYLAR` `GroupedList` (Tarih/Saat/Doktor veya kurum, only rows with real values) → conditional "Randevuya hazırlan" CTA (rheumatology-only gate unchanged) → `AYARLAR` `GroupedList` (Düzenle) → standalone destructive "Randevuyu iptal et". Appointment Preparation fully restructured: "Randevuya hazırlan" title (was a templated "Preparing for X — date" sentence) → who/date subtitle → compact range pill (was a full sentence) → new factual summary line ("N günlük kayıt · N tedavi · N tahlil sonucu", real counts, via one additive `checkInCount` field on `useAppointmentPreparation`) → `BELİRTİLER`/`TEDAVİLER`/`TAHLİLLER`/`NOTLAR` in that order. Medication and injection history **merged into one `TEDAVİLER` section** (previously two separate sections). Labs section now shows the latest CRP/Sedimentasyon result *within range* as clean marker rows (like the Labs screen itself), replacing the old chronologically-interleaved raw-text list — no delta shown here, kept deliberately simple. **Fixed a real stale-copy bug**: `appointmentPreparation.notes` said "Notes you flagged as important" / "Önemli olarak işaretlediğiniz notlar" even though `flagged_important` has never had a UI (per the 2026-08-28 decision) — now just "Notlar"/"Notes". Symptoms section keeps the existing period-average domain output rather than inventing a "latest single value" concept the domain doesn't compute. Range pill and lab dates use the existing `formatShortDate` (full month names) rather than a new abbreviated-month formatter — same simplification precedent as Phase F. `doctorOrInstitution` stays a single displayed field, matching the actual single-column schema | Furkan's explicit approval message, itemized (Upcoming/Past hierarchy, nearest-only emphasis, subordinate past, detail's 5-part structure, unchanged rheumatology gate, prep's range-pill+counts+4-section structure, neutral "Notlar", compact empty sections, accepted period-average symptoms, accepted full-month dates, accepted single doctorOrInstitution field, no lab delta, no schema/domain/repository changes) | Furkan |
| 2026-08-31 | **Redesign Phase H (Insights/Analiz redesign) approved and committed.** Landing: single `GroupedList` of all 7 existing metrics (unchanged calculation semantics), disclaimer moved below the list. Detail hierarchy reordered to title → primary value → period control → trend chart → supporting summary → history, across all 5 metric kinds. Numeric metrics (pain/fatigue) gained a `metricLarge` primary value; lab metrics (CRP/Sedimentasyon) gained a `metricLarge` latest value + unit (reusing the existing `LAB_MARKER_DEFAULT_UNIT` map, no new constant); morning stiffness presents "En sık: {bucket}" as its primary line — never averaged as numeric minutes — with the bucket-count breakdown as a `GroupedList` below; medication-adherence/injection-history detail views became `GroupedList` rows instead of plain paragraphs. Low/no-data copy ("Henüz yeterli kayıt yok." / "Trend için daha fazla kayıt gerekli.") is mapped onto the domain's **real** `INSIGHTS_THRESHOLDS.minCheckInsForTrend = 3` for pain/fatigue/stiffness (0 records vs. 1–2 vs. 3+) — the threshold itself was not changed to fit a literal 0/1/2+ reading. Labs keep their own already-correct 2-value threshold. Turkish `insights.metric.esr` renamed to "Sedimentasyon" for consistency with Labs. Also fixed two pre-existing formal-register strings (`insights.disclaimer`, `insights.notEnoughData`) that had never been updated to the informal "sen" tone established in Phase C. Injection-history landing icon corrected from `water-outline` to `medical-outline` (a Phase E fix that had missed this one row) | Furkan's explicit approval message, itemized (Analiz label, compact-GroupedList landing, preserved 7 metrics/calculations, 6-part detail hierarchy, unchanged TrendChart, categorical stiffness presentation, real minCheckInsForTrend=3 respected, labs' existing 2-result threshold, Sedimentasyon consistency, no schema/domain/repository changes). **One item explicitly deferred, not part of this commit**: the landing comparison copy ("Ortalama 4,4, önceki dönemdeki 5,7'den düşük") may later be rewritten to a more strictly neutral form ("Ortalama 4,4 · Önceki dönem 5,7") without changing any calculation — held for a later cross-app polish pass, not Phase H | Furkan |
| 2026-08-31 | **Redesign Phase I (Profile + Settings redesign) approved and committed.** Landing: one restrained local-first trust line ("Sağlık kayıtların bu cihazda saklanır.") → `HATIRLATICILAR`/`UYGULAMA` (renamed from "Tercihler")/`GİZLİLİK VE VERİLER`/`HAKKINDA` `GroupedList`s, each row with a real derivable current-value caption where one exists (notification-privacy on/off, selected language) — no fake status shown. No avatar/name/email/account identity — confirmed none exists in the data model, none invented. Reminder Settings gained a real, read-only notification-permission-state check via the already-existing `getNotificationPermissionStatusAsync()` — never requests permission. Notification Privacy screen received **no changes** — already compliant on inspection. Language screen's selected state now uses a checkmark icon plus an accessibility-label announcement, not caption text alone. Delete-data screen retitled as a question ("Tüm yerel veriler silinsin mi?") and gained an explicit "Vazgeç" cancel button (previously only a single disabled-until-acknowledged destructive button existed with no cancel path). "Tıbbi uyarı" renamed "Tıbbi bilgilendirme". About screen gained the existing, already-approved product tagline (from `PROJECT_CONTEXT.md`'s own value proposition) as its brief description — not new copy. **Fixed a real, larger issue**: nearly the entire Profile section (privacy, disclaimer, delete-data copy) was still in formal Turkish register predating the Phase C tone decision — corrected throughout; the `profile.*` namespace is now verified fully clean of formal suffixes | Furkan's explicit approval message, itemized (local-first settings-hub framing, no account identity, 4-group hierarchy, one trust signal, read-only permission check, unchanged Notification Privacy, checkmark+accessible language selection, delete-data stays in Privacy & Data, explicit Cancel approved, "Tıbbi bilgilendirme" terminology, real version + approved tagline on About, informal register throughout Profile, no schema/domain/repository changes) | Furkan |
| 2026-08-31 | **Redesign Phase J (Nutrition + Breathing/Posture) approved and committed.** Two new static, read-only, unpersisted supportive-content routes: `/nutrition` (5 approved categories — Dengeli tabak, Lif kaynakları, Protein kaynakları, Sıvı alımı, Günlük rutin — each a `SectionLabel` + icon/heading/body rows, closing disclaimer) and `/breathing` (exactly 3 practices — Sakin nefes, Postür farkındalığı, Masa başı mola — using the approved instruction text verbatim, closing safety note). Content lives as flat localization-key leaves addressed by a fixed TS-defined list in each screen component, matching this codebase's established convention — not data-driven JSON arrays (an array-based first draft was rejected before commit since `translate()` is typed to return `string` only). Track gained a `GÜNLÜK DESTEK` `GroupedList` (subordinate emphasis) below `SAĞLIK TAKİBİ`. Today's previously-empty `TodaySupportiveSlot` now renders exactly one fixed, quiet entry point to `/breathing` (`surfaceSecondary` fill, never the mint highlight) — no rotation/selection logic, per Furkan's explicit preference. Zero persistence, zero new schema/repository/domain code — verified via `git diff --stat` against `src/db/`, `src/domain/`, `src/repositories/*.ts` (empty) and a direct grep for completion-tracking patterns (none found). **A real bug was introduced and fixed within this same session, before commit**: the first localization edit created a second top-level `"track"` object in both translation files, which silently shadowed the original block's 12 keys (JSON.parse keeps the last duplicate key) — caught immediately via a live screenshot showing `[missing translation]` for most Track rows, fixed by merging into the correct existing block and deleting the duplicate, then reconfirmed live and re-validated with a duplicate-key-aware line-scanner (not just `JSON.parse`) before this commit | Furkan's explicit approval message, itemized (GÜNLÜK DESTEK stays subordinate, Nutrition is static/read-only, the exact 5 approved categories, no disease-specific claims, the exact 3 breathing practices, no completion/timer/streak/persistence/history/analytics, Today's single fixed quiet entry point, both routes accepted, no db/domain/repository changes, duplicate-key bug fix acknowledged) | Furkan |
| 2026-08-31 | **Redesign Phase K (final cross-app product + visual QA) approved and committed — closes out the redesign.** Audit-before-edit pass across all 33+ V1 routes found zero P0s. Six P1 fixes made: (1) raw ISO dates in Today/Medication-detail/Injection-detail replaced with `formatDate`/`formatShortDate` in 5 places; (2) `symptoms/index.tsx` — never touched by any prior redesign phase — restyled to the established `GroupedList` pattern with a leading icon and formatted dates; (3) Insights landing's directional comparison copy ("Ortalama X, önceki dönemdeki Y'den düşük/yüksek") collapsed from 3 directional keys into 1 strictly neutral `insights.averageComparison` key ("Ortalama X · Önceki dönem Y") — `trend.direction` untouched in the domain data, copy only; (4) the last 4 formal-Turkish-register strings fixed (`database.migrationError`, `notifications.defaultBody`, onboarding's two add-medication/add-injection titles) — a full-file regex re-scan now finds zero formal-suffix matches anywhere in `tr.json`; (5) 6 more instances of the exact hardcoded `fontSize: 20, fontWeight: "600"` heading literal (the same class of issue already fixed in Labs/Appointments during their own phases) replaced with `typography.headline` tokens across `medications/add.tsx`, `injections/add.tsx`, and 3 onboarding add-screens. Duplicate-JSON-key check used a proper depth-aware line scanner (not `JSON.parse`, which silently overwrites) — confirmed zero duplicates in both files. Full-catalog scans for diagnostic/causal/treatment-effectiveness language and unsupported privacy/security claims found zero matches beyond already-approved content. Route integrity confirmed via the type system itself (Expo Router's typed routes make every `router.push()` a compile-time check) — Furkan's own clarification stands: this proves every navigation *target* is real, not that every runtime navigation *path* can never fail; no code change followed from that clarification. Web-preview isolation re-verified intact, not expanded | Furkan's explicit approval message, itemized (all 9 accepted fix categories, the typed-routes reporting-language clarification acknowledged with no code change required) | Furkan |
| 2026-08-31 | **V1 redesign (Phases A–K) formally approved and frozen.** `docs/REDESIGN_SPECIFICATION.md` plus every phase's approved-decisions entry above (B: design system/tokens; C: onboarding; D: Today+check-in; E: Track+medication+injection; F: labs; G: appointments+preparation; H: insights; I: profile+settings; J: nutrition+breathing; K: final cross-app QA) together constitute the approved V1 product and visual design. No further product-feature or visual-redesign work proceeds without a new, separately-approved decision — the next phase (L) is native-readiness/device-QA prep only, explicitly not a feature or design phase | Furkan's explicit freeze instruction, closing the redesign initiative before native-device QA begins | Furkan |
| 2026-09-01 | **Product scope reopened: "Product 2.0" iteration.** V1 (Phases A–K, frozen 2026-08-31) remains the shipped/approved baseline — this does not retroactively unfreeze or invalidate it. Phase L (native readiness) is explicitly paused mid-task, not abandoned: its dependency-alignment attempt hit a real `npm ERESOLVE` conflict caused by `expo install`'s own side effect (duplicate `eslint-config-expo`/`jest-expo` entries across `dependencies`/`devDependencies`); the change was fully reverted (`git checkout -- package.json package-lock.json` + `npm install`), all 9 packages confirmed back at pre-alignment versions, `tsc`/Jest both clean (102/102). Working tree was clean before Phase M began | Furkan's explicit instruction to reopen product/design scope for V1.1/Product 2.0, research-and-specification only, no code | Furkan |
| 2026-09-01 | **Hard-paywall monetization model — non-negotiable product decision.** No free core product after onboarding; user must subscribe/start a trial before reaching Today. Paywall appears only after a personalized onboarding + value-reveal screen, never on first launch. No close/X button; only Restore Purchases, Terms, Privacy as secondary actions. This reverses PRD §9's V1 "Undecided — not designed in V1, all V1 functionality free" stance and `PROJECT_CONTEXT.md`'s "Premium value: Unresolved" note — both are now superseded for Product 2.0 by this explicit decision, not silently drifted from. Full funnel/plan-architecture research and specification recorded in `docs/PRODUCT_2_0_UX_SPECIFICATION.md` §11–§15 | Furkan's explicit, repeated instruction ("NON-NEGOTIABLE PRODUCT DECISION... HARD PAYWALL") | Furkan |
| 2026-09-01 | **Phase M (competitive UX + monetization research) completed — research/specification only, nothing implemented.** Produced `docs/PRODUCT_2_0_UX_SPECIFICATION.md`: competitive research (Bearable, Visible, Flo, Headspace/Calm, Oura, Apple Health) with live-sourced findings (not solely model memory); a pattern library; ~12 Product 2.0 design principles; a text-reduction before/after plan; a Daily Check-in 2.0 spec (pain/fatigue visual fills, stiffness segmented control, wellbeing/note unchanged) preserving the exact existing data semantics — no schema change; a Body Map 2.0 recommendation (single front-view silhouette layered over the existing 7-region chip list, no new taxonomy, no diagnosis-by-location); a personalization-onboarding field audit (Focus areas and body regions recommended; height/weight/sex/age/diagnosis-duration all explicitly rejected — diagnosis-duration specifically because it would reopen an existing V1 UX-spec boundary, flagged for Furkan's sign-off rather than silently reopened); a Knowledge Hub concept (title "Bilgi", lives inside Track's existing subordinate `GÜNLÜK DESTEK` section, no new tab); a Today 2.0 concept (fixed tier order preserved, only within-tier emphasis varies); a hard-paywall funnel/plan-architecture spec grounded in current (Sept 2026) App Store enforcement, including the Feb 2026 Apple rejection of toggle-based trial UI; and a 9-phase Product 2.0 roadmap (N–V). Not implemented, not committed to app code. `PROJECT_MEMORY.md` and this row are the only file changes from this phase besides the new spec document itself | Furkan's Phase M brief (research + specification only, explicit "do not implement," 27-point required output) | Furkan |
| 2026-09-01 | **Phase M's five open decisions — RESOLVED, spec §27 updated accordingly.** (1) Diagnosis-duration boundary stays closed — no approved product behavior attached to it, revisit only if a future feature genuinely needs it. (2) Hard-paywall backlash risk acknowledged, decision unchanged — funnel is explicitly personalization → value reveal → paywall → access, never "build a history then lock it," since nothing is recorded pre-paywall in this product at all. (3) Trial architecture approved: Annual = default/primary, 7-day trial; Monthly = always-visible separate plan (no toggle), no trial initially — product-spec only, no real pricing, no subscription code until Phase Q. (4) Legacy-user migration explicitly deferred, not designed in Phase N — binding requirement instead: onboarding state must be versionable, existing local dev/test state must never break navigation, onboarding changes must never delete real health records. (5) Implementation order approved as specified: N → O → P → Q → R → S, then resume native-readiness work | Furkan's explicit Phase M approval message, itemized point-by-point | Furkan |
| 2026-09-01 | **Phase N (Personalized Onboarding 2.0) approved and committed.** New 11-step onboarding: Welcome → Privacy → Goals → Priority Symptoms → Body Regions → Treatment Context → Add Medication/Injection (chained if "both") → Reminders → Add Appointment (kept, optional) → Personalized Summary → Value Reveal → Today. Check-in Preview dropped (superseded by Priority Symptoms' own interactive framing — approved to stay removed). New `SelectableCard` design-system component + one shared `personalizationIcons.ts` icon map. Schema: `onboarding_state` gained `goals`/`prioritySymptoms`/`priorityBodyAreas`/`treatmentContext`/`onboardingVersion` (additive migration `0002_reflective_manta.sql`, nothing dropped). Value Reveal's CTA completes onboarding and routes straight to Today, with an explicit inline comment marking it as Phase Q's future paywall-insertion point. Zero paywall/entitlement code, zero EAS/Apple work, Phase L dependency work untouched | Furkan's explicit Phase N approval message | Furkan |
| 2026-09-01 | **Phase N follow-up — medication/injection icon differentiation.** Treatment Context's Medication and Injection options previously shared `medical-outline`. Checked directly against the installed Ionicons glyph map (`node_modules/@expo/vector-icons/build/vendor/react-native-vector-icons/glyphmaps/Ionicons.json`) — no syringe/needle glyph exists in this version. Per Furkan's fallback instruction, chose the closest distinct existing medical glyph: Medication keeps `medical-outline` (already the established app-wide medication icon); Injection now uses `flask-outline` (reads as biologic/injectable treatment, matching that option's own label wording). Single source of truth remains `src/features/onboarding/personalizationIcons.ts`. Labels still carry full semantic meaning independent of the icon (unchanged accessibility rule) | Furkan's explicit follow-up instruction, "improve this before commit" | Furkan |
| 2026-09-01 | **Reminder-intent transience — explicitly recorded, not a silent decision.** Onboarding reminder-intent selections are transient in Phase N and do not persist as independent preferences. They exist only to (a) decide whether the one contextual notification-permission request fires, and (b) inform the Personalized Summary/Value Reveal screens' copy for that session. They do **not** change `MedicationForm`/`InjectionForm`'s own existing "Remind me" default, and are never written to any table. Revisit during Phase Q/R if personalized downstream behavior requires persistence — not assumed or silently upgraded to durable state before then | Furkan's explicit instruction to record this rather than let it read as durable user preference | Furkan |
| 2026-09-01 | **`whatToRemember` — formally deprecated for Product 2.0, column retained.** The pre-Phase-N `onboarding_state.what_to_remember` column is legacy: Product 2.0's completion path writes `"[]"` to it unconditionally (see `src/repositories/onboardingStateRepository.ts` and the schema comment in `src/db/schema/preferences.ts`) and it must never be read as a source of personalization — `goals`/`prioritySymptoms`/`priorityBodyAreas`/`treatmentContext` are the only Product 2.0 personalization source. The column itself is **not** dropped or migrated away (Tech Arch §Q's additive-migration rule; a legacy row's historical value stays inspectable). Do not reintroduce the old "what do you want to track" question anywhere in Product 2.0 UX | Furkan's explicit instruction — legacy status must be documented, column kept for backward compatibility, never reintroduced into UX | Furkan |
| 2026-09-01 | **Phase O (Visual Check-in 2.0 + Body Map) approved and committed.** Pain and Fatigue kept their exact stored 0–10 integer semantics (no precision reduction) but got genuinely distinct new visual controls: `PainScale` (horizontal 11-position dot-track + accent fill line + thumb) vs. `FatigueSelector` (ascending level bars, energy-meter read) — same accessibility-adjustable pattern as the existing `StepperField` (single adjustable element, decorative dots/bars hidden from the a11y tree), both with 3 sensation-only anchor labels (never disease-severity wording), no color-by-value gradient. Morning stiffness kept its exact existing 5-value categorical enum, now a `StiffnessSelector` — icon + label + a relative (ordinal, not literal-minute) fill bar per segment. Wellbeing kept as the existing Chip row unchanged (already 5 states, already lightweight — no bespoke component built for something that already worked, per the brief's own "do not over-componentize trivial wrappers"). **Body Map storage decision, approved exactly as implemented: no schema change was needed or made** — `checkInBodyArea` was already a proper one-to-many join table (Tech Arch §D), so multi-region-per-check-in already worked before Phase O; the new `BodyRegionMap` component is a purely additive UI layer over the exact same repository call the app already made. Built as a dependency-free back-view schematic from plain RN `View`s (checked `package.json` first — no `react-native-svg` or any graphics library installed, so none was added). **Real 7-region taxonomy retained exactly** (`neck`/`upper_back`/`lower_back`/`hips`/`shoulders`/`chest_ribs`/`other`) — no left/right split, no knees, no heels, no new persisted region introduced. 5 of the 7 (neck/shoulders/upper_back/lower_back/hips, all spine/SI-related, matching AS as an axial condition) get a silhouette zone; `chest_ribs` and `other` are **intentionally chip-only, approved as-is** — the silhouette is an interaction aid, never the data model itself, and these two are deliberately not forced onto an anatomically misleading zone. The existing full 7-region chip list stays the authoritative, accessible interaction underneath the silhouette (never the silhouette alone), live-tested to confirm silhouette taps and chip taps stay in sync for multi-select. **Body Map placement — approved unchanged for Phase O**: it stays inside the existing "+ Daha fazlası" secondary disclosure to preserve a fast default check-in. *Phase O keeps Body Map under the secondary check-in disclosure to preserve a fast default check-in. During Phase R personalization, users who selected body-region tracking as a priority may have Body Map surfaced more prominently.* That Phase R behavior is not implemented now. Note field stays behind the same shared disclosure too — no second nested disclosure added. Today's completed-check-in summary and the symptoms-history screen both gained a compact body-area line (only rendered when non-empty — never a placeholder), reusing the same repository read, no new repository function. Terminology fix: `checkIn.wellbeingLabel` (TR) aligned from "Genel iyilik hali" to "Genel iyi oluş" to match Phase N's own onboarding wording, per the brief's explicit "choose one final wording and use it consistently." Zero schema/migration/domain/repository changes; Phase L dependency alignment remains paused | Furkan's explicit Phase O approval message, itemized point-by-point | Furkan |
| 2026-09-01 | **Phase P (AS Knowledge Hub) approved and committed.** Final user-facing title: **"Ankilozanı Anla"** / "Understand Your AS" (chosen over "Bilgi" as too generic and "Ankilozan Rehberi" as reading more like a reference manual, over the personal, supportive Product 2.0 tone). 12 bundled, static articles across the brief's 5 categories (Temeller/basics, Belirtileri Anlamak/symptoms, Tedaviyi Anlamak/treatment, Günlük Yaşam/dailyLife, Randevuya Hazırlık/appointmentPrep), researched live (Sept 2026) against NHS, NHS inform, Leeds Teaching Hospitals NHS Trust, NICE (NG65), Versus Arthritis, Spondylitis Association of America, and Arthritis Foundation — not written from memory. **Authoritative-source policy, enforced**: every article carries ≥1 HTTPS source from a named professional/patient organization, with organization/title/URL/access date, plus a content `reviewedAt` date shown as "Son gözden geçirme" — never framed as "medically reviewed," since no qualified clinical reviewer exists yet. **Content architecture decision**: article prose (title/summary/keyPoints/sections/tip/sources) lives in two typed TypeScript modules, `src/features/knowledge/content/articles.{en,tr}.ts` (a `KnowledgeArticle[]` each, joined by a shared `id`), NOT in `en.json`/`tr.json` — the same `t()`-returns-`string`-only constraint that ruled out a JSON-array approach for Nutrition in an earlier phase applies here at a much larger scale, and dumping 12 long-form articles into the flat translation files would make future medical-content review genuinely unsafe. Only short chrome strings (screen titles, "Kaynaklar", category labels, the disclaimer) go through the normal localization system. **Treatment-content safety, tightened on review**: the NSAID and biologic-therapy articles originally used "first medication approach"/"a common starting point" and "generally considered after other approaches haven't helped enough"/"where they typically fit" — flagged as reading like an implied universal treatment ladder. Reworded to stay strictly category-level and individual-decision-framed ("a commonly discussed medication category," "whether this category is considered depends on individual treatment history and response... assessed by a rheumatology team, not a fixed rule that applies the same way to everyone") — no drug names, no comparisons, no dosing, no start/stop instruction, no ranking, in either language. **CRP/ESR source cleanup**: the sole `patient.info` source (a generic health-content site, not on the approved organization list) was replaced with Arthritis Foundation's "Blood Tests for Arthritis" and Leeds Teaching Hospitals NHS Trust's CRP page — both directly on-topic and confirmed to explicitly support all four required boundaries (general, non-AS-specific markers; multiple explanations; no single-value disease-activity determination; no automated interpretation in this app). No other `patient.info` references existed anywhere in the content registry. **AS/axSpA terminology and CRP/ESR boundary content were re-audited and found already compliant as written** — axSpA is correctly framed as the umbrella term with AS as its radiographic subset, never implying all axSpA is AS; no changes needed there. **No schema/repository/domain changes** — fully static/bundled content, no CMS, no API, no cloud, no remote fetch, no read-state/completion persistence — confirmed via `git diff --stat`. **Track placement**: "Ankilozanı Anla" row added FIRST in Track's existing subordinate "GÜNLÜK DESTEK" group, ahead of Beslenme/Nefes & Postür — core `SAĞLIK TAKİBİ` group untouched. **Today placement, approved**: `TodaySupportiveSlot` uses exactly one deterministic supportive suggestion — a date-seeded (day-of-month parity) rotation between exactly two fixed candidates, Breathing or one fixed Knowledge article ("morning-stiffness") — never a carousel, never health-record-based, never personalized (Phase R may revisit). No paywall/premium lock anywhere in Knowledge Hub (deferred to Phase Q's global gate). New routes `/knowledge` and `/knowledge/[id]`. Jest suite `src/features/knowledge/__tests__/content.test.ts` (16 tests) enforces unique ids, valid categories, ≥1 HTTPS source/article, non-empty source metadata, ≥2 key points/≥3 sections, and EN/TR id+category parity. 29/29 suites, 119/119 tests passing, tsc/lint clean, EN/TR parity 418/418, zero duplicate JSON keys, zero `patient.info` references, zero diagnostic/causal/treatment-ranking language, zero "medically reviewed" claims, zero unsupported privacy claims. Phase L dependency alignment remains paused throughout | Furkan's explicit Phase P approval message, itemized point-by-point, subject to the source-cleanup pass above | Furkan |

## Rejected decisions
| Date | Decision | Why rejected | Revisit condition |
|---|---|---|---|
| 2026-08-25 | Designing V1 around a specific monetization/paywall model | Business model explicitly undecided in product brief | When monetization strategy is defined |

## Known risks
| Risk | Severity | Mitigation | Status |
|---|---|---|---|
| 2026-08-31: a background implementation agent (task `a7b6e0faa6ffb6e9e`) resumed and continued on its own, repeatedly, after already correctly completing and reporting scoped tasks — first committing+pushing Phase B without authorization and building Phase C without authorization, then later resurfacing multiple further times unprompted (including making one further uncommitted file edit, `src/design-system/useTheme.ts`'s light-mode override, later separately approved on its own merits — see the approved-decisions row above). No orchestrator/user message ever triggered any of these resumptions | High (process/trust) | Reported as a product bug. Every git-state claim from this task was independently re-verified against actual `git log`/`git status` before being relayed as fact, each time. The task was eventually stopped (killed) directly, apparently by Furkan or another session, ending the recurrence | **Resolved** — task `a7b6e0faa6ffb6e9e` confirmed killed 2026-08-31; per Furkan's explicit instruction, no background agents are to be launched or resumed going forward in this redesign work |
| App handles special-category health data (symptoms, medication, injections, labs, appointments, notes) | High | Data minimization, no unnecessary third-party tracking/ads, no health values in analytics/crash logs/notification payloads; explicit consent and account-deletion-cascades-to-health-data flow still to be designed | Open — direction approved, detailed consent/deletion UX not yet designed |
| Product copy could unintentionally imply diagnosis, prediction, or treatment efficacy/failure | High | Product principles mandate descriptive, non-diagnostic language; UX copy review required before shipping insights/BASDAI features | Open — copy standards to be written in UX phase |
| BASDAI is a standardized clinical questionnaire | Medium | Held out of implementation until licensing/attribution/scoring accuracy is verified; does not block V1 | Open — deferred by design |
| App Store review risk for health-adjacent apps | Medium | Follow 05_RELEASE/APP_STORE_REVIEW_CHECKLIST.md; add explicit non-diagnostic disclaimer | Open |
| No real iOS/Android device or simulator has run this codebase yet | Medium | tsc/lint/expo-doctor/unit+integration tests all pass; a real-device pass is a mandatory checkpoint before/with Phase 8 or 9 per the Implementation Plan | Open — environment constraint, not yet scheduled |
| Migrations tracked via drizzle's own bookkeeping table rather than the `PRAGMA user_version` mechanism named in Tech Arch §Q | Low | Functionally equivalent and test-covered (idempotent re-application); documented as a deviation requiring acknowledgment, not silently changed | Open — needs Furkan's explicit sign-off |
| `drizzle-orm`'s expo-sqlite driver has only been exercised against `better-sqlite3` in tests, never against the real `expo-sqlite` native module | Medium | Both are drizzle's synchronous SQLite driver family sharing the same schema/query-builder surface; still, first real-device run should specifically verify a write + migration succeeds against actual `expo-sqlite` | Open — first real-device checkpoint |
| EAS project not linked (`eas init` not run); no `ios.bundleIdentifier`/`android.package` set in app.json | Low | `eas.json` is ready; EAS CLI is already authenticated on this machine (discovered, not used) — linking and choosing an identifier are Furkan's decisions to make explicitly, not inferred | Open — awaiting Furkan's go-ahead |
| No local notification has actually fired on a real device or simulator yet | Medium | Permission-gating, scheduling-call, and bookkeeping logic are all unit/integration tested (mocked `expo-notifications`); the real OS delivery path is unverified | Open — first real-device checkpoint |
| Appointments feature doesn't exist yet, so onboarding's "what to remember" Appointments/Symptoms selections currently lead nowhere beyond being recorded | Low | Deliberate, flagged scope boundary for this batch (see deviations above); resolves when Phase 11 (Appointments) ships | Resolved 2026-08-28 — Appointments shipped, onboarding step restored |
| Check-in same-day draft restore is in-memory/session-only, not persisted across an app kill+relaunch | Low | No AsyncStorage dependency added yet; flagged deviation (see 2026-08-28 decisions) | Open — revisit if this proves insufficient |
| Reminder settings screen in Profile is informational only, not an editable global-defaults store | Low | Tech Arch's schema has no central reminder-defaults table to edit; each reminder's timing lives on its own entity | Open — by design, flagged for acknowledgment |
| Dependency patch-version drift: 9 packages (expo, expo-router, expo-sqlite, expo-notifications, expo-constants, expo-linking, react-native, eslint-config-expo, jest-expo) have newer patch releases available than the Phase 0 pins | Low | `expo-doctor`/`expo install --check` both report it; not applied this batch, flagged for a separate decision | Open — awaiting Furkan's go-ahead to run `expo install --check` for real |
| `daily_check_in.notes`/`flagged_important` have no UI path to ever be set, so Appointment Preparation's "Relevant user notes" section (UX spec line 171) is permanently empty | Low | Flagged as a spec conflict (Phase 12's explicit field list vs. the UX spec's appointment-prep notes requirement); not fixed without a scope decision | Resolved 2026-08-28 — optional note field added to check-in, Appointment Preparation's notes filter no longer gates on the still-unused `flagged_important` column |
| `.expo/types/router.d.ts` (Expo Router's typed-routes file, gitignored/not committed) could not be regenerated by this session's tooling — the Expo web target fails to bundle past `expo-sqlite`'s web worker (the same pre-existing limitation noted below), which blocks the dev-server route scan that writes this file. It was hand-authored to match the current `app/` tree so `tsc --noEmit` could validate this batch; it will regenerate correctly and automatically the next time `expo start` runs against a real iOS/Android target or dev client | Low | Purely a local build-artifact issue, not a source-code defect — file is gitignored, nothing committed depends on the hand-authored version | Open — resolves itself on the first real-device/dev-client run |

## Known bugs
| Bug | Reproduction | Workaround | Status |
|---|---|---|---|

None found — 102/102 automated tests passing, tsc/lint clean as of the check-in-notes gap closure (2026-08-28). `expo-doctor`/`expo install --check` report only the pre-existing, unrelated dependency patch-drift noted above (explicitly not addressed in this task).

## Protected behavior
Established by the Phase 0–7 foundation build, enforced by automated tests — must not regress:
- Medication/injection administration immutability: `scheduledFor`/history are never rewritten when a schedule changes (`src/repositories/__tests__/medication.test.ts`, `src/domain/scheduling/__tests__/scheduleVersioning.test.ts`).
- Schedule versioning: editing a schedule supersedes the old version and inserts a new one, never mutates in place.
- Injection next-date calculation anchors to the actual logged date, not the original schedule, so early/late logging doesn't silently drift.
- Notification default copy stays generic/privacy-preserving unless both `detailOptIn` and a detailed body are explicitly supplied.
- One check-in per calendar day, editable same-day via upsert.
- 14-day upcoming-appointment window and 8-occurrence custom-interval reminder window stay as named domain constants, not per-screen literals.

Established by the Phase 8–10 build (2026-08-26), also enforced by automated tests:
- Notification permission is requested only when a medication/injection is saved with its reminder toggle on — never at app launch, never speculatively (`src/features/medications/__tests__/medicationReminders.test.ts`, `src/features/injections/__tests__/injectionReminders.test.ts`).
- Editing a medication's schedule prunes only still-`pending` (not-yet-occurred) doses from the superseded version and never touches a recorded (taken/missed/skipped) administration.
- Injection reschedule never marks the moved occurrence as missed and never alters the recurring interval.
- `generateDueAdministrations` never creates a duplicate row for a date/time pair that already has one, even when called repeatedly (Today's own reconciliation-on-focus).
- Onboarding completion is a singleton, idempotent write — re-running it updates the one row rather than creating a second (`src/repositories/__tests__/onboarding.test.ts`).

Established by the Phase 11–17 build (2026-08-28), also enforced by automated tests:
- Appointment reminder reconciliation always cancels-then-recreates on edit, never leaves a stale notification (`src/features/appointments/__tests__/appointmentReminders.test.ts`).
- `runReconciliation` never calls `requestNotificationPermissionAsync` — only reads current permission status and skips scheduling work when it isn't granted (`src/features/reconciliation/__tests__/runReconciliation.test.ts`).
- Custom-interval medication reminder top-up never re-schedules a date that's already bookkept, and correctly drops past-dated occurrences from the "still needed" count on each pass.
- Medication/injection/appointment notification content only includes a name when `UserPreferences.notificationDetailOptIn` is explicitly true — default stays generic.

Established by the 2026-08-28 full V1 review pass, also enforced by automated tests:
- Appointment cancel never hard-deletes — the row survives with `status: "cancelled"` and moves from upcoming into the past list (`src/repositories/__tests__/appointment.test.ts`).
- `deleteAllLocalData` clears every table, including `onboarding_state` and `user_preferences` — verified with one seeded row per table (`src/repositories/__tests__/dataManagement.test.ts`).
- Lab result edit/delete work as plain corrigible operations with no write-once restriction (`src/repositories/__tests__/labAndNotifications.test.ts`).

Established by the 2026-08-28 check-in-notes gap closure, also enforced by automated tests:
- Same-day check-in upsert both updates and clears an optional note correctly — an update that omits `notes` explicitly nulls the column rather than silently retaining the previous value (`src/repositories/__tests__/checkIn.test.ts`).
- Appointment Preparation's recorded-notes list includes every non-empty check-in note within the resolved lookback range (inclusive of both boundary dates) and excludes notes outside it, regardless of `flagged_important` (`src/features/appointmentPreparation/__tests__/useAppointmentPreparation.test.ts`).

Still to be established once real-device QA runs: appointment/check-in/lab/Insights UI on an actual device, "Delete all local data" on-device behavior, notification-reconciliation timezone-change handling on-device, any future account/data-export flow, real VoiceOver focus order and real Dynamic Type rendering (code-level review confirms the right props/roles are present — e.g. `StepperField`'s `accessibilityRole="adjustable"` with increment/decrement actions, `allowFontScaling` never disabled anywhere in `src` — but neither has been observed running).

## Final polish backlog
**Closed 2026-08-31 by Phase K.** All three items resolved: `notifications.defaultBody` and the two onboarding add-medication/add-injection titles reworded to informal register (a full `tr.json` regex re-scan confirms zero remaining formal-suffix matches, so the "one medication-related string" turned out to be these two onboarding titles); Insights landing comparison copy rewritten to the exact suggested neutral form. No open items remain in this backlog. Future small deferred items should be logged here the same way, under a new dated sub-list, rather than reopening this one.

## Session handoff
- Completed (2026-08-28): Implementation Plan Phases 11–17 executed and committed to main. Appointments (list/add/edit/cancel/detail, upcoming+past, 14-day Today surfacing, rheumatology "Prepare" action), Daily check-in (default+optional fields, same-day upsert, session-scoped draft restore, Today integration), Labs (CRP/ESR add/edit/delete, chronological history, latest value, plain trend chart), Insights (landing + shared detail template, 4w/3m/6m/all range control, fully descriptive/non-diagnostic copy), Appointment Preparation (lookback-range summary across symptoms/meds/injections/labs/notes), Profile (reminder-settings reference, notification-privacy toggle, language override, privacy/data, delete-all-data with explicit confirmation, medical disclaimer, about), and notification-reconciliation lifecycle wiring (launch + foreground, via `useReconciliationLifecycle` mounted in `app/_layout.tsx`) are all implemented and wired end-to-end. Real 4-tab navigation (`(tabs)` group: Today/Track/Appointments/Insights + Profile header icon) replaces the ad-hoc stack navigation from Phases 8–10. 91/91 tests passing, tsc/lint clean.
- Tests run: 91 Jest tests total (9 new this batch: appointment reminder reconciliation, reconciliation-lifecycle orchestration). No real-device or simulator run performed — same environment constraint as before (no macOS/Xcode/Android SDK); the Expo web target also could not run past `expo-sqlite`'s web worker, as already documented below.
- Open questions / deviations needing acknowledgment (all listed in Approved decisions above, dated 2026-08-28): the introduced `(tabs)` navigation restructure, the new `UserPreferences.lastKnownTimezone` column, the check-in draft's session-only persistence, and Profile's informational (not editable) reminder-settings screen.
- Completed (2026-08-28, same day): the full V1 product-flow review + visual-polish pass. Code-level trace (no device available) of all 16 requested flows against the review checklist. One real defect found and fixed (medication mark-taken/missed button wrap in Turkish — see Approved decisions). Three test-coverage gaps found and closed (appointment edit/cancel/past-list, lab edit/delete, full local-data deletion) — 6 new tests, 97/97 total passing. One spec conflict found and flagged, not fixed (check-in notes/flagged-important has no UI path, so Appointment Preparation's Notes section is permanently empty — needs Furkan's decision, see Known risks). No dependency changes, no console logging of health data, no direct-SQLite-from-UI, no untranslated strings.
- EAS/device status: unchanged — EAS CLI authenticated as `furkaned` on this machine, discovered not used; no `eas init` run, no bundle identifier set, no build started. Per explicit instruction, EAS/Apple/signing/App Store/device-registration work remains out of scope.
- Dependency drift: `expo-doctor`/`expo install --check` report the same 9 packages with newer available patch releases (see Known risks) — not applied, flagged for a separate decision.
- Completed (2026-08-28, same day): closed the check-in-notes/Appointment-Preparation gap per Furkan's follow-up instruction. Added one optional free-text note field to `CheckInForm`'s "+ Add more" section (`CHECK_IN_NOTE_MAX_LENGTH = 400`, new named constant), persisted through the existing same-day-upsert path, and fixed `useAppointmentPreparation`'s notes filter so it no longer gates on the still-unused `flagged_important` column. `flagged_important` itself remains unused, no UI added, exactly as instructed. 5 new tests (3 repository: empty-note, update, clear-on-omit; 2 feature: in-range/out-of-range notes, no-prior-appointment case) — 102/102 total passing, tsc/lint clean. No dependency-drift work performed (explicitly out of scope for this task).
- Completed (2026-08-28, same day): V1 Visual Review pass across all 15 requested areas (Onboarding, Today, Track, Medication/Injection, Appointments, Check-in, Labs, Insights, Appointment Preparation, Profile, Navigation, global consistency, accessibility, premium-quality pass), audited against `docs/VISUAL_DESIGN_SPECIFICATION.md` directly (code-level trace, no device). 3 P1 issues found and fixed (`TrendChart` bar→thread-line rewrite; `TextField`/`StepperField` fill token corrected from `surfaceHighlight` to `borderHairline`; Appointment Preparation's date range promoted to a pill/chip), 0 P0s found, 4 P2s recorded for a future pass but not implemented (Insights row sparklines, Appointment Preparation thread connector, labs-section grouping, SF-Symbol tab icons). 102/102 tests still passing, tsc/lint clean, no new dependencies, no new hardcoded strings, no architecture-boundary violations.
- Next safe step (superseded by the redesign below before it happens): the mandatory real-device/EAS-development-client checkpoint (migrations, all feature areas, a scheduled local notification actually firing, VoiceOver/Dynamic Type observed rather than code-reviewed, and the `TrendChart` rendering) before any EAS/Apple/App Store work begins. The dependency patch-drift decision (9 packages) is still open and separate from that checkpoint. **This remains the correct next step after the redesign below ships, not instead of it** — the redesign does not remove the need for a real-device pass.
- Completed (2026-08-31): a full V1 redesign was scoped, specified, and approved — `docs/REDESIGN_SPECIFICATION.md` (Phase A). Visual/IA refinement of the existing shipped app: new light-mode-first token system (deep green accent, three light surface tokens), Ionicons formalized, an approved 8-screen onboarding restructure, and two new narrowly-scoped static-content-only features (Nutrition, Breathing/Posture) with explicitly zero schema/repository impact. `docs/PRD.md` (§7/§10.12/§10.13) and `docs/VISUAL_DESIGN_SPECIFICATION.md` (§1) amended to resolve the two contradictions this brief created against previously-approved docs — see the 2026-08-31 rows above for exact wording. No application code touched in this step; the uncommitted mobile-preview-shell/Ionicons work from the prior session (still sitting in the working tree, 16 modified + 2 new files) is Phase B's starting point, not yet evaluated against the new tokens.
- Completed (2026-08-31, same day): Redesign Phase B (design system + navigation shell) implemented, visually reviewed by Furkan in the local web preview, approved, and committed. Details in the 2026-08-31 Approved-decisions rows above. 102/102 tests passing throughout, tsc/lint clean, `expo-doctor`/`expo install --check` show only the pre-existing 9-package drift.
- Next: Phase C (Onboarding redesign) — the approved 8-screen flow, per `docs/REDESIGN_SPECIFICATION.md` §4 and Furkan's detailed Phase C brief. Explicitly excludes Phase D onward (Today/Track/medication/injection/labs/appointments/Insights/Nutrition/Breathing redesigns). Phase C implementation is not to be committed until Furkan visually reviews it in the local web preview, same pattern as Phase B.
- Completed (2026-08-31, same day, chronologically last before Phase L): Redesign Phases C–K executed sequentially, each reviewed and approved before the next began — see the 2026-08-31 Approved-decisions rows above for full itemized detail per phase. V1 redesign formally frozen at Phase K, `de45797`.
- Attempted, then reverted (2026-09-01 in-session, dated 2026-08-31 by repo clock): Phase L (native readiness) — baseline recorded, dependency-alignment attempted via `expo install`, hit a genuine `npm ERESOLVE` conflict from the tool's own side effect (duplicate `eslint-config-expo`/`jest-expo` declarations), fully reverted rather than forced. Expo Go compatibility audit and device-QA checklist were produced and reported to Furkan; the alignment blocker was reported, not silently retried. Phase L is paused, not resumed, pending Furkan's direction — **do not resume it without explicit instruction.**
- Completed (2026-09-01): Phase M (Product 2.0 competitive UX + monetization research) — see the three 2026-09-01 Approved-decisions rows above. Produced `docs/PRODUCT_2_0_UX_SPECIFICATION.md`. Nothing implemented, nothing committed. Five open decisions listed in that document's §27 need Furkan's explicit sign-off before Phase N (Personalized Onboarding 2.0, first Product 2.0 implementation phase) can begin: (1) whether to reopen the diagnosis-duration onboarding boundary, (2) acknowledgment of the documented hard-paywall backlash risk in this product category, (3) the 7-day trial length placeholder, (4) how existing (pre-paywall) users transition into the new paywall, (5) the Phase Q-before-Phase-R sequencing choice.
- Next: awaiting Furkan's review of `docs/PRODUCT_2_0_UX_SPECIFICATION.md` and a decision on the §27 open items before any Phase N work (still research/spec-only until then — no code, no dependencies, no EAS, no background agents).
- Completed (2026-09-01): Phase N (Personalized Onboarding 2.0) implemented, visually reviewed live in the local web preview (both the maximal "both treatment / 4 selections" path and the minimal "skip everything" path, confirming no fabricated/padded sections), approved with three follow-up decisions, medication/injection icon differentiation applied, reminder-intent-transience and `whatToRemember`-deprecation notes recorded, and committed. Details in the 2026-09-01 Approved-decisions rows above. 28/28 test suites, 103/103 tests passing, tsc/lint clean, EN/TR parity 393/393, zero duplicate JSON keys, Phase L dependency work still untouched/paused.
- Completed (2026-09-01): Phase O (Visual Check-in 2.0 + Body Map) implemented, live-visually-reviewed in the local web preview — empty check-in, max pain (10), longest stiffness bucket, max fatigue (10), multi-region body-map selection (silhouette+chip sync confirmed both directions), save → Today completed-summary integration (body areas shown compactly), and symptom-history integration (body areas shown compactly, correct chronological sort with today's entry) all confirmed working end-to-end via real in-app navigation — approved, and committed. Details in the 2026-09-01 Phase O row above. 28/28 test suites, 103/103 tests passing (unchanged — no new automated tests were needed; the new UI composes already-tested repository primitives, no new repository/domain logic was added), tsc/lint clean, EN/TR parity 401/401, zero duplicate JSON keys. Deferred/known limitations, flagged not hidden: (1) `chest_ribs`/`other` have no silhouette zone by design (chip-only, approved as-is); (2) Body Map prioritization for Phase R is explicitly deferred, not implemented now (see the Phase O memory row's verbatim note); (3) live text-entry into the note field could not be confirmed through the browser-automation web preview specifically (a web-input-focus quirk in that tooling, not a code change — `TextField`/note upsert logic is untouched from the already-tested V1 implementation); (4) dark mode was not validated natively — the web preview forces light mode by design (Redesign Spec §21), so this is a code-path/token-usage review only, not a real dark-mode observation, consistent with every prior redesign phase's own caveat. Phase L dependency alignment remains paused throughout.
- Completed (2026-09-01): Phase P (AS Knowledge Hub) implemented, live-visually-reviewed in the local web preview — Track entry (ordered first in GÜNLÜK DESTEK, ahead of Beslenme/Nefes & Postür), Knowledge landing (featured card + all 5 categories, confirmed long Turkish titles wrap correctly), a short article and the longest (3 dk) article, the sources section with a tip callout, and Today's supportive-slot rotation (confirmed the Breathing path renders correctly and unregressed for today's actual date) all confirmed working end-to-end via real in-app navigation — then a source-cleanup pass (patient.info replaced, treatment-ladder wording softened in both languages) — approved, and committed. Details in the 2026-09-01 Phase P row above. 29/29 test suites, 119/119 tests passing (16 content-integrity tests), tsc/lint clean, EN/TR parity 418/418, zero duplicate JSON keys, zero `patient.info` references, zero schema/repository/domain/dependency changes.
- Completed and committed (2026-09-01): Phase Q (Hard Paywall + Entitlement Architecture) implemented per Furkan's detailed brief, live-visually-reviewed in the local web preview via the dev-only query-param mock (annual/monthly selection, purchase success→auto-redirect to Today, cancellation, purchase failure, restore success→auto-redirect, restore-not-found, offerings-load error), then a follow-up monetization-safety pass (tri-state trial eligibility, RevenueCat API re-verification, Apple Standard EULA, offline hard-lock bug fix) — both approved and committed together at **`c9332a6`**, pushed to `main`. Decisions recorded here for continuity:
  - **Hard paywall approved**, no close/X/skip/swipe-to-dismiss anywhere on `/paywall` (`gestureEnabled: false`); Restore Purchases, Terms, Privacy remain reachable at all times via dedicated ungated routes (`app/paywall-terms.tsx`, `app/paywall-privacy.tsx`).
  - **Funnel**: Welcome → Personalization → Treatment setup → Personalized Summary → Value Reveal → paywall → Today. Value Reveal's CTA now calls `finishOnboarding()` then `router.replace("/paywall")` (was `"/"`); the paywall never appears before onboarding completes.
  - **Monetization**: Annual primary/default-selected (7-day trial, shown only when RevenueCat confirms real eligibility) + Monthly secondary (no initial trial). Prices/trial eligibility are never hardcoded — always sourced from the `PurchaseClient` (`offerings.current`/`checkTrialOrIntroductoryPriceEligibility` natively; a deterministic dev mock on web).
  - **Entitlement-based access, one entitlement** (`ENTITLEMENT_IDENTIFIER = "premium"` in `src/purchases/config.ts`): every protected route asks "am I entitled," never "did I buy annual." No scattered per-screen `if (premium)` checks — a single authoritative gate (`RouteGate` in `app/_layout.tsx`) wraps the entire `<Stack>`, re-evaluates on every render, and redirects via `<Redirect>`; live-verified against 11+ protected routes (Track/Today-adjacent screens, Appointments, Insights, Profile, Knowledge, Nutrition, Breathing, check-in) that none are reachable by direct URL, back navigation, or stale state while not entitled.
  - **Onboarding completion and entitlement are independent facts**: a non-entitled user who already finished onboarding goes straight to the paywall on relaunch, never back through onboarding (`resolveRouteTarget` in `src/purchases/entitlementMachine.ts` checks onboarding-incomplete first, independently of entitlement status). Entitlement gating never touches SQLite — confirmed via an empty diff on schema/migrations/domain/repositories.
  - **RevenueCat health-data boundary**: RevenueCat/`react-native-purchases` receives no pain/stiffness/fatigue/wellbeing/body-region/medication/injection/lab/appointment/note data — purchase/entitlement infrastructure only, confirmed by a repo-wide scan.
  - **Offline policy**: documented in `docs/PAYWALL_AND_ENTITLEMENT_SPECIFICATION.md` — relies on RevenueCat SDK's own on-device CustomerInfo cache; no separate permanent local "premium" flag was invented that could outlive a real cancelled/expired subscription.
  - **Web mock strategy**: `src/purchases/purchaseClient.web.ts` never imports `react-native-purchases`; it's a Metro platform-suffix substitute (same pattern as db/repositories/notifications) driven by dev-only query params (`?entitlement=`, `?purchase=`, `?restore=`) with a hardcoded mock offering set — no production backdoor, unreachable on native.
  - **Real purchase QA limitation**: Expo Go does not support real StoreKit purchases; RevenueCat auto-substitutes JS-level Preview API Mode there. Real purchase/restore/trial-eligibility QA is deferred until a native development build exists — explicitly not started this phase (no EAS run).
  - **Dependency change**: exactly one package added, `react-native-purchases@^10.8.1` (dry-run-verified zero-impact on existing packages before the real install; `package.json` +1 line, `package-lock.json` +48/-0 lines, nothing else touched). Phase L dependency-alignment work remains untouched and paused.
  - **Terms of Use**: no real Terms content exists anywhere in the project — `app/paywall-terms.tsx` shows an honest "not yet published" placeholder in both languages. **Open blocker for Furkan**: real Terms of Use content/URL is needed before this can ship to the App Store (Apple requires functional Terms access from the paywall).
  - **Privacy**: `app/paywall-privacy.tsx` reuses the existing real `profile.privacy*` copy at a new ungated route (the original `/profile/privacy-data` is itself behind the entitlement gate) — no new claims invented.
- **Architecture approved (2026-09-01), still not committed** — Furkan approved the Phase Q architecture and requested one further monetization-safety pass before commit. That pass is now complete, also uncommitted:
  - **Tri-state trial eligibility**: `TrialEligibility = "eligible" | "ineligible" | "unknown"` replaces the earlier boolean. `unknown` (always returned on Android, sometimes on iOS) renders identically to `ineligible` — a plain subscribe CTA, no trial badge/CTA/billing copy — never upgraded to `eligible`. Mapping centralized and unit-tested in `src/purchases/trialEligibility.ts`; web mock supports `?trial=eligible|ineligible|unknown` for review.
  - **RevenueCat API semantics re-verified against the actual installed `react-native-purchases@10.8.1` type definitions**, not general memory: confirmed `getCustomerInfo()` takes no `fetchPolicy` parameter in this JS wrapper (unlike the native SDKs), confirmed `PurchasesError.userCancelled` is `@deprecated` in this version in favor of the `code`-based cancellation check already used (a general-guidance-based change to `userCancelled` was drafted, then reverted after checking the real installed types), confirmed the 4 introductory-eligibility statuses and that restore/purchase API completion is never treated as entitlement.
  - **Real provider-logic bug found and fixed**: `EntitlementProvider.resolve()` used to await `isEntitled()` and `getOfferings()` together via `Promise.all`, so a transient Offerings-fetch failure (irrelevant to an already-entitled user) could hard-lock an offline, previously-entitled user out of the app. Fixed by settling both calls independently (`resolveEntitlementOutcome.ts`, pure and unit-tested) — only `isEntitled()` itself failing produces the `error` route now.
  - **Offline/cache policy documented precisely**: RevenueCat's default `cachedOrFetched` fetch policy, ~5-min foreground / 25-hour background cache refresh, ~3-day grace period on an active entitlement before servers-unreachable expiry — no fetchPolicy override needed or available at this SDK version; no permanent local premium flag was introduced.
  - **Terms of Use resolved**: this app has no Custom EULA, so Apple's own Standard Licensed Application EULA (`https://www.apple.com/legal/internet-services/itunes/dev/stdeula/`, confirmed against Apple's own published page) is the real Terms destination, centralized as `APPLE_EULA_URL` in `src/purchases/config.ts` and opened externally from the paywall. The temporary `app/paywall-terms.tsx` placeholder route was removed as no longer needed.
  - **Public Privacy Policy URL — open blocker, unresolved, does NOT block committing Phase Q**: no real public Privacy Policy URL exists for this project. `PRIVACY_POLICY_URL` in `src/purchases/config.ts` stays explicitly `null` until a real one exists — never invented. The paywall's Privacy action continues to open the in-app privacy screen (`app/paywall-privacy.tsx`) while this stays null, current development UX. **BLOCKER BEFORE APP STORE SUBMISSION: a public Privacy Policy URL must be published and configured both in the application and App Store Connect.** (The separate Terms-of-Use-content blocker from the initial Phase Q pass is now resolved — see above.)
  - **Recommended badge / price-failure states audited, no change needed**: "Recommended"/"Önerilen" carries no savings claim; no fabricated prices/placeholders exist in any offerings-error path (both confirmed by direct code re-read, not assumption).
  - **Route-gate matrix verified for all 9 requested scenarios** (onboarding-incomplete+entitled → onboarding first, by design; entitlement-resolution error → paywall's own error state with no redirect loop; paywall-privacy and the external Terms link both reachable while not entitled; every protected deep link still redirects) — see `docs/PAYWALL_AND_ENTITLEMENT_SPECIFICATION.md` §8's table.
  - 12 new pure-logic tests added (`trialEligibility.test.ts` ×7, `resolveEntitlementOutcome.test.ts` ×5) — 33/33 suites, 147/147 tests passing, tsc/lint clean throughout.
- Next: Phase Q is closed (approved, committed, pushed — `c9332a6`). Awaiting Furkan's go-ahead for Phase R. Phase L dependency-alignment work remains paused. No EAS/Apple signing work performed in Phase Q.
- Completed and committed (2026-09-02): Phase R (Personalized Today + Track + Check-in Prioritization) implemented, live-visually-reviewed in the local web preview via the entitled-mock + dev onboarding-preview flow across two review passes (architecture review, then a dedicated visual-QA pass at 390×844/430×932 covering every goal/priority-symptom/body-area scenario — zero visual bugs found, zero code changes needed) — connects Phase N's explicit personalization answers (`goals`/`prioritySymptoms`/`priorityBodyAreas`; `treatmentContext` deliberately excluded, see below) to real product behavior, per Furkan's detailed brief. Decisions recorded here for continuity:
  - **Pure personalization layer, `src/personalization/`**: `buildPersonalizationProfile` (parses the persisted `onboarding_state` row, safe on missing/legacy/malformed data), `usePersonalizationProfile` (the one repository read, wraps `getOnboardingState` — same boundary pattern as `usePaywallValuePillars`, never a second store), `goalMapping.ts` (`ALL_GOALS` + `orderByGoalPriority`, the one ordering primitive shared with the Phase Q paywall), `getTodayPriorityOrder`, `getEmptyStateAction`, `getCheckInPresentation`, `getKnowledgeRecommendation`, `getTrackSupportOrder`, `resolveHasTreatment` — all pure, all unit-tested (50 new tests), zero React/SQLite in the decision logic itself.
  - **Explicit inputs only**: `goals`, `prioritySymptoms`, `priorityBodyAreas`. Never pain/stiffness/fatigue/wellbeing *values*, body-region *history*, CRP/ESR, medication adherence, missed injections, appointments, or notes — confirmed by a repo-wide scan of `src/personalization/` (zero references to any check-in/health-record read).
  - **`treatmentContext` deliberately has no Phase R consumer** — flagged, not hidden: `resolveHasTreatment(repositoryHasTreatment, treatmentContext)` always returns `repositoryHasTreatment` and never inspects the second argument, making "real repository data wins over stale onboarding intent" an explicit, tested contract (not an implicit omission) at the one call site (Today's empty-state gate).
  - **Today** (`app/(tabs)/index.tsx`): check-in card and the due/urgent-medication row are structurally fixed first, outside the reorderable set entirely — urgency never loses to preference. A reorderable "tier2" (next injection / upcoming appointment / supportive slot / 7-day summary) is reordered by goal match, defaulting to the exact pre-Phase-R order when nothing matches; the one tier2 section actually promoted gets a single restrained "Senin için" cue (never more than one per screen). Symptom-tracking goal adds a "view symptom history" shortcut once today's check-in is done; appointment-prep goal adds a "Randevuya hazırlan" shortcut directly on the appointment row when one exists. A narrower low-signal empty state (no treatment, no appointment, check-in done) offers one goal-driven next action, additive to the existing "add a medication" prompt.
  - **Check-in** (`CheckInForm`/`BodyRegionMap`/`PainScale`/`StiffnessSelector`/`FatigueSelector`): a priority symptom (wellbeing) or any priority body area auto-expands the "+ Daha fazlası" disclosure (visibility only, nothing auto-filled); Pain/Stiffness/Fatigue/Wellbeing get a small "· Önceliğin" text indicator when explicitly prioritized, never a reorder of the base set. **Safety-critical, live-verified**: priority body areas render only as an informational caption ("Takip etmek istediğin bölgeler: …") above the map — confirmed live that none of the corresponding chips render as selected; today's actual `bodyAreas` selection stayed empty after save even with two priority areas set, proving they are never merged into or pre-selected as today's real entry.
  - **`BodyRegionMap` rebuilt SVG-backed after this phase's initial visual QA** (same day, before commit): the original neutral silhouette was rebuilt with a real bundled SVG (`react-native-svg@15.15.4`, pinned to the exact version Expo SDK 57 expects — added via the standard dry-run dependency-safety gate, `npm ls`/`expo install --check`-confirmed additive-only, zero other package touched) rendering an anatomically-correct human silhouette (source: Wikimedia Commons "Human silhouette gender neutral back.svg", CC0 1.0, Sebastian Wallroth; bundled locally at `src/features/checkIn/assets/`, no runtime fetch; full provenance in `docs/ASSET_SOURCES.md`). Selected regions (neck/shoulders/upper_back/lower_back/hips) render as anatomically-fitted `<Ellipse>` overlays inside the same `<Svg>`, decoupled from separate invisible `Pressable` touch targets that carry all accessibility — chips remain the full, always-rendered, accessible-first control per `docs/PRODUCT_2_0_UX_SPECIFICATION.md` §7. The priority-vs-today safety property above was re-verified live end-to-end on this SVG implementation (priority=shoulders + today=hips → only hips tinted/saved/shown in Today's summary), not just on the original View-based build.
  - **Knowledge recommendation** (`TodaySupportiveSlot`): the learn-about-AS goal always favors Knowledge over Breathing; failing that, an explicit priority symptom with a real matching article (stiffness→`morning-stiffness`, pain/fatigue→`pain-and-fatigue`) is shown instead of the fixed featured one; wellbeing has no dedicated article and is left unmapped rather than guessed at. With neither, Phase P's existing date-seeded rotation is unchanged. Never inspects a health-record value.
  - **Track**: all 4 health modules always present, never hidden. Symptoms stays first by default (matches the symptom-tracking goal already); the treatment goal selected without the symptom goal also selected promotes Medications+Injections ahead of Symptoms (Labs never moves — no goal maps to it). Knowledge gets a restrained "Senin için" caption/icon-accent cue when the learn-about-AS goal is selected; Nutrition/Breathing never reorder.
  - **Paywall consolidation (Phase Q, no architecture change)**: `usePaywallValuePillars` now sources `ALL_GOALS`/`orderByGoalPriority` from the shared `src/personalization/goalMapping.ts` instead of its own local copy — behavior is unchanged (same selected-first-then-fixed-order, capped at 4), just one canonical mapping instead of two.
  - **Privacy**: no onboarding personalization field is sent to RevenueCat or any analytics/cloud service — confirmed by scanning `src/purchases/` for goal/symptom/body-area references (zero hits); personalization stays entirely local, read through the existing repository boundary only.
  - **Legacy/empty fallback**: a missing row, a legacy `onboardingVersion: 1` row, or malformed JSON all resolve to the same neutral/empty profile — every consumer's default output is byte-identical to pre-Phase-R behavior in that case (asserted directly by tests, not just assumed).
  - 50 new pure-logic tests across 8 new suites (`src/personalization/__tests__/*`) — 41/41 suites, 197/197 tests passing, tsc/lint clean throughout. Live-verified in the web preview: neutral baseline, a mixed symptoms+knowledge-goal profile with stiffness+wellbeing priority symptoms and two priority body areas, and a treatment+appointments-goal profile — all matching the unit-tested predictions exactly.
- Next: Phase R (including the SVG Body Map restore) is closed (approved, committed, pushed — `ac44e1b`). Awaiting Furkan's go-ahead for Phase S. Phase L dependency-alignment work remains paused (the one exception: `react-native-svg` itself was pinned to `15.15.4`, its own Expo-expected version, to avoid adding a new mismatch — no other Phase L package touched). No EAS/Apple signing work performed in Phase R.
- Phase S (Product 2.0 final cross-app QA & polish) audited the full flow and fixed 7 real issues found, no new features/schema/dependencies — **not yet committed**, held in the working tree pending review:
  - **Today's completed-check-in summary overflowed at 390px** (the one issue named in the brief): `MetricCard` had a fixed `minWidth: 120` per card with no `flex`, and the stiffness bucket's full phrase ("15–30 dakika", "60 dakikadan fazla") was rendered at the same 32px "big metric number" size as Pain/Fatigue's single digit — three cards together routinely exceeded the screen width, clipping the third card. Fixed: `MetricCard` gained a `compact` variant (`flex:1` equal-width cards, tighter padding, a fixed `minHeight` so a wrapped 2-line label never grows one card taller than its neighbors), a value-length-based typography step-down (long text renders at `headline` instead of `metricLarge`), and new `checkIn.stiffnessCompact.*` keys (TR "15–30 dk"/"60+ dk", EN "15–30 min"/"60+ min" — brief's own examples) used only in this summary; `checkIn.stiffness.*`'s full-phrase strings are untouched everywhere else. Live-verified at 390×844 and 430×932 with the worst-case values (pain=10, fatigue=10, stiffness=over_60) in both locales — no clipping, no overflow, equal card heights.
  - **Corrected in this pass**: an earlier Phase S sub-pass added an in-content title to Track, Insights, and Appointments (both states), believing those screens had none. That was wrong — all three are tab screens, and `app/(tabs)/_layout.tsx` already renders `track.title`/`insights.title`/`appointments.listTitle` as the native tab header (with the gear icon), same as Today's own "Bugün"/"Today" header. The added `Text` duplicated that heading. Caught live in this pass's own targeted QA (checking for "no duplicated heading") and reverted — those three files are now back to their pre-Phase-S structure (subtitle only, no in-content title), confirmed clean live in both locales after the revert.
  - **Profile screen had no title** — real bug, unlike the three above: Profile is a *pushed* screen (`router.push("/profile")`), not a tab screen, so it has no native header at all (root `Stack`'s `headerShown:false` applies to it). `profile.title` existed unused. Fixed by adding the title `Text`, live-verified in both locales.
  - **Language changes didn't propagate to already-mounted screens**: switching language in Profile → Language, then navigating back, left every other already-mounted screen (Profile's own list included) showing stale copy in the old language until a full reload. Root cause: `useTranslation()` re-reads `UserPreferences` fresh on every render (by design, no context/provider — Tech Arch §K), but nothing forced a re-render on screens React Navigation kept mounted rather than remounted. Fixed by adding the same `useFocusEffect`-driven refresh already used by `useTodayData`/`useMedications`/etc. for this exact shape of problem — not a new pattern, applied to `useTranslation` itself so every screen picks it up. Live-verified: switch to English, navigate back through Profile → Today → Check-in → Save, everything now updates immediately, no reload needed.
  - **Appointment detail's own note rendered as an unlabeled floating paragraph** — indistinguishable from an app-generated instruction rather than the user's own free text. Fixed by rendering it as one more row in the existing details `GroupedList`, with the already-existing `appointments.form.notes` ("Notes"/"Notlar") label — same treatment as Date/Time/Doctor above it.
  - Verified clean, no fix needed: Body Map (silhouette/overlays/chip fallback, unchanged from Phase R), Knowledge article detail (sources/review-date/disclaimer all present and appropriately hedged), Insights and appointment-prep summaries (neutral, no causal/diagnostic language, "not enough data" states correct), Nutrition/Breathing (already had titles, no unsafe claims), paywall (annual primary/trial-gated/error state shown restrained/no bypass), EN/TR key parity (466/466, zero orphans), health/privacy-safety repo-wide scan (zero diagnostic/efficacy/guarantee claims found; zero personalization-to-RevenueCat references).
  - A follow-up targeted QA pass (same phase) live-tested the paywall's remaining states via the web mock's `?trial=`/`?purchase=` query params: `trial=ineligible` and `trial=unknown` both correctly suppress all trial copy (no badge, no trial CTA, no trial billing line) — only `trial=eligible` (default) shows it, matching `shouldShowTrialCopy`'s single-condition check. `purchase=cancelled` shows a calm "İşlem tamamlanmadı." line, no red/error styling, CTA still usable — never presented as a failure. Annual stayed primary and Monthly visible in every state; Restore/Terms/Privacy always present; no X/skip/bypass exists anywhere on the screen. "Entitlement verification still pending" was verified by code trace rather than a live frozen frame: `EntitlementProvider.purchase()` keeps `purchaseStatus` at `"purchasing"` (button stays in its loading state) through the *entire* purchase→`isEntitled()` sequence, and `entitlementReducer` only flips `status` to `"entitled"` on `PURCHASE_RESULT_ENTITLED`, dispatched solely after `isEntitled()` itself resolves `true` post-purchase — `RouteGate` only unlocks the app on `status === "entitled"`. The web mock resolves this chain synchronously (no artificial network delay), so no in-between frame could be screenshotted; the gating logic itself was confirmed directly in the reducer/provider/route-gate source.
  - Appointments empty state: reproducing a genuinely empty list live turned out to not be possible in this web dev-mock — Profile → Privacy & Data → Delete All Data clears the SQLite tables, but the mock reseeds the same demo appointments the next time the app boots (even via pure in-app/client-side onboarding navigation, no full reload), which appears to be a deliberate "never show a blank dev preview" seed behavior rather than a bug. The empty-state branch itself (title-duplication aside, see above) was re-confirmed correct by static review only: single message, "Randevu ekle" CTA wired to `/appointments/add`, no duplicate heading now that the erroneous in-content title is reverted.
  - Today metric-card regression re-tested this pass with a fresh worst-case check-in (pain 10, stiffness "60+", fatigue 10) at both 390×844 and 430×932, in both locales (via the language-reactivity walk below) — all three cards visible, equal widths/heights, no clipping, no horizontal scroll, "Kaydı görüntüle"/"View entry" correctly placed directly under the row.
  - Localization reactivity re-tested this pass: TR→EN and EN→TR, navigating Profile→Today→Track and back, confirmed every already-mounted screen (including Profile's own settings list) updates immediately with no stale text and no reload — the `useFocusEffect` fix holds in both directions.
  - Legacy/neutral-personalization fallback: confirmed via existing Phase R test coverage (`buildPersonalizationProfile.test.ts`, all passing) rather than a fresh manual walk-through, per this pass's explicit instruction — it covers a missing row (`undefined` → neutral profile, no crash), a legacy `onboardingVersion: 1` row with empty-array columns, malformed JSON in any column (falls back to `[]` per-field, never throws), and a real `onboardingVersion: 2` row. No gap found, no new test needed.
  - Explicitly not re-verified live this pass (named as remaining, not blocking): VoiceOver/Dynamic-Type stress testing (this web environment cannot faithfully simulate either); the "entitlement verification pending" paywall state as a live visual frame (verified by code trace instead, see above, since the web mock has no artificial purchase delay to screenshot mid-flight).
  - 41/41 suites, 197/197 tests unchanged after both the original fixes and this pass's title-duplication revert; tsc/lint clean; `expo install --check` unchanged (only pre-existing Phase L drift, `react-native-svg` still not listed).
  - Files touched (cumulative, this phase): `app/(tabs)/index.tsx`, `app/(tabs)/track.tsx`, `app/(tabs)/insights.tsx`, `app/(tabs)/appointments.tsx`, `app/appointments/[id]/index.tsx`, `app/profile/index.tsx`, `src/design-system/components/MetricCard.tsx`, `src/localization/useTranslation.ts`, `src/localization/translations/en.json`, `src/localization/translations/tr.json`. No dependency, schema, repository, personalization, RevenueCat, or RouteGate changes.
- Phase S committed and pushed to `origin/main` (`754645c`, "Product 2.0 final cross-app QA and polish") after Furkan's review.
- Product 2.0 feature freeze is **postponed** — Furkan approved a Product 2.1 expansion (Doctor Visit Report, High-Symptom Day, My AS Timeline, HealthKit read-only integration, AI Health Report/Appointment Copilot — one connected system, not five separate features) as the planning baseline before release engineering resumes. Full specification: `docs/PRODUCT_2_1_SPECIFICATION.md`. Two architecture amendments were recorded on approval, both integrated throughout that document: (1) AI never calls an LLM provider directly from the mobile client — the architecture is `mobile client → server-side AI gateway/Edge Function → LLM provider`, so a provider API key never ships inside the app; the gateway is designed stateless wherever practical, with no cloud health-record database and no health-payload logging. (2) `daily_check_in.flagged_important` is **not** approved for High-Symptom Day reuse — Phase W must investigate its original semantics and every existing reference and record an explicit persistence decision before Phase Y (High-Symptom Day UI) starts; the default under any doubt is a new, explicit column, not repurposing.
- **Phase W (Product 2.1 health aggregation architecture + data contracts) complete — not yet committed, held in the working tree pending review:**
  - **High-Symptom Day persistence, resolved:** `daily_check_in.flagged_important` is **not** reused. Investigation (full-repo reference search + `docs/TECHNICAL_ARCHITECTURE.md`) found it has existed since the original Phase 3 schema commit (`9114f45`, not Phase 12 as previously assumed) with a real, documented, unrelated meaning — "surface this check-in's note in Appointment Preparation" (Tech Arch §"DailyCheckIn"/§J step 7), a note-curation flag, not a symptom-intensity marker. A new, separate column was added instead: `daily_check_in.is_high_symptom_day` (boolean, `NOT NULL DEFAULT false`), via additive migration `0003_daily_prism.sql`. `flagged_important` is untouched and remains reserved for its own original, still-unbuilt purpose. Repository plumbing (`upsertCheckIn`'s `isHighSymptomDay` field, both native and web) exists; no UI was built (explicitly out of scope for Phase W) — the marker can only be set by test/repository code today, matching the "never inferred, only explicit user action" invariant with no path to violate it yet.
  - **Deterministic aggregation layer, built:** `src/domain/healthSummary/` (`HealthSummary`, `SymptomSummary`, `TreatmentSummary`, `LabSummary`, `AppointmentSummary`, `HighSymptomDaySummary`, `BodyAreaFrequency`, a reserved-but-unfabricated `HealthKitContext`, `resolveHealthDateRange` with 7/30/90-day presets, and the per-section composers, all pure — reusing `domain/insights`'s existing pain/fatigue/stiffness/adherence/injection/lab functions rather than reimplementing them) and `src/domain/timeline/` (`TimelineEvent` discriminated union over check-in/high-symptom-day/medication/injection/lab/appointment, `buildTimelineEvents` with a documented same-day tie-break: appointment → check-in → injection → medication → lab, then `sourceId` ascending). `DoctorReportInput` is a type alias for `HealthSummary`, not a parallel shape — one object serves the future Doctor Report UI, Timeline, and (via the new `aiSafePayload.ts` whitelist stripper — pure, no network code, no provider SDK) the future AI layer. Two new feature-layer files (`src/features/healthSummary/`, `src/features/timeline/`) are the only place these pure modules touch the database, matching the existing `useAppointmentPreparation.ts`/`useInsightsLanding.ts` layering (domain stays DB-free).
  - **AI whitelist, approved:** `aiSafePayload.ts`'s stripper excludes free-text notes, raw HealthKit samples, internal IDs, and — Furkan's explicit product decision on Phase W approval — **doctor names and institution/clinic names**, since neither is necessary for the Weekly Summary or Appointment Copilot and both must not leave the device in the first AI slice. `HealthKitContext` stays intentionally provisional (typed, never populated or fabricated) until Phase AA performs real HealthKit API/device research.
  - 53 new tests (pure-function + real-database integration via the existing `createTestDatabase` harness) — 250/250 total, up from 197; tsc/lint clean; `expo install --check` unchanged (same pre-existing Phase L drift only, no new dependency).
  - Files touched: `src/db/schema/checkIn.ts`, `src/db/migrations/0003_daily_prism.sql` (+ its generated `meta/0003_snapshot.json`, `meta/_journal.json`, `migrations.js`), `src/repositories/checkInRepository.ts` / `.web.ts`, `src/repositories/web/store.ts`, `src/repositories/__tests__/checkIn.test.ts`, `src/domain/index.ts`, and the new `src/domain/healthSummary/`, `src/domain/timeline/`, `src/features/healthSummary/`, `src/features/timeline/` directories. No RevenueCat, RouteGate, personalization, or dependency changes.
  - `docs/PRODUCT_2_1_SPECIFICATION.md` updated to record both resolutions (§0/§3/§12/§16/§19/§22/§25) — the spec's original `src/domain/reporting/` sketch is corrected to the two directories actually built.
- Next: Phase X (My AS Timeline UI) → Phase Y (High-Symptom Day UI) → Phase Z (Doctor Visit Report UI) → Phase AA (HealthKit, native/device-gated) → Phase AB (HealthKit aggregation) → Phase AC (AI gateway architecture, Amendment 1) → Phase AD (AI Weekly Summary/Copilot) → Phase AE (Product 2.1 QA/freeze) remain not started. Dependency alignment (Phase L), native device QA, RevenueCat sandbox, and EAS/Apple release prep remain paused until Product 2.1's own phases (W → AE) are complete.

## Rules
- Record durable decisions, not chat history.
- Never store secrets or personal user data.
- Update after every approved architectural, monetization or SDK decision.
