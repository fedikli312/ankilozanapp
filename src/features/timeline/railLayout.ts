/**
 * Shared rail geometry (Design System 2.0, Phase Design-E) — one source of
 * truth so the rail line, event markers, and day markers all agree on
 * exactly where the vertical line sits, in every consumer (the full
 * Timeline screen and the Health Record landing's compact preview).
 */
export const RAIL_GUTTER_WIDTH = 28;
export const RAIL_LINE_X = RAIL_GUTTER_WIDTH / 2;
export const RAIL_LINE_WIDTH = 1.5;
